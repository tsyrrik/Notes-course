# OpCache и работа интерпретатора
PHP код проходит лексер → парсер → транслятор (opcodes) → VM (Zend Engine). Обычный запрос компилируется заново, OpCache сохраняет opcodes между запросами для ускорения.
