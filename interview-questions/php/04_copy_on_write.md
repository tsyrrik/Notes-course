# Copy-on-Write
PHP копирует значения лениво: при присвоении хранится одна копия, реальное копирование происходит только при изменении.
```php
$a = [1, 2, 3];
$b = $a;   // копии данных нет
$b[] = 4;  // теперь создаётся копия для $b
```
Экономит память и время при передаче больших структур.
