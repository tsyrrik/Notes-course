# include vs require
- `require` — фатальная ошибка, если файл не найден (обязательный файл).
- `include` — предупреждение, выполнение продолжается (необязательный файл).
Используйте *_once, чтобы избежать повторного подключения.
