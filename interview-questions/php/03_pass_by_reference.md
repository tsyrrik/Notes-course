# Передача по ссылке
Ссылка позволяет менять исходную переменную через другую.
```php
$b = 1;
$a = &$b; // $a и $b указывают на одно zval
$b = 2;
echo $a; // 2
```
Используется для экономии копирования массива/объекта или для функций, которые должны менять аргумент.
