# Задачи на синтаксис
- Ссылки: `$a=&$b; $b=2; echo $a; //2`
- `unset($a); echo isset($a); // false`
- Статическая переменная в функции: `foo(); //10, затем 11`
- Сравнение строк и чисел: `"0" == 0 // true`
- Приведение пустого массива: `(bool)[] // false`
