### Что это

**RPC (Remote Procedure Call)** — это способ, при котором ты вызываешь **функцию на другом сервере**,  как будто она локальная.  
Не “ресурс” как в REST, а **действие**.  
Фокус не на “что” (пользователь, заказ), а на “что сделать” (создать, удалить, посчитать).

---

### Пример на пальцах

REST говорит:

```
POST /users
{
  "name": "Mikhail Savin"
}
```

RPC говорит:

```
POST /createUser
{
  "name": "Mikhail Savin"
}
```

И сервер буквально вызывает функцию `createUser()`.

---

### Ключевая идея

- Вызов метода = запрос к серверу.
    
- Параметры передаются в теле запроса.
    
- Сервер возвращает результат выполнения.
    
- Протокол (HTTP, TCP, JSON-RPC, gRPC — неважно) — просто транспорт.
    

RPC — это **прозрачная функция через сеть**.

---

### Разновидности

| Вариант      | Формат                | Пример                                        |
| ------------ | --------------------- | --------------------------------------------- |
| **JSON-RPC** | JSON через HTTP       | `{"method": "sum", "params": [1,2], "id":1}`  |
| **XML-RPC**  | XML через HTTP        | `<methodCall><methodName>sum</methodName>...` |
| **gRPC**     | Protobuf через HTTP/2 | Быстрый бинарный вариант от Google            |
| **SOAP**     | XML + WSDL            | Тяжёлый корпоративный наследник RPC           |

---

### Пример JSON-RPC запроса

```json
{
  "jsonrpc": "2.0",
  "method": "getUser",
  "params": { "id": 10 },
  "id": 1
}
```

Ответ:

```json
{
  "jsonrpc": "2.0",
  "result": { "id": 10, "name": "Mikhail Savin" },
  "id": 1
}
```

Всё строго: `"method"` → имя функции, `"params"` → аргументы, `"result"` → ответ.  
Никаких `/users/10` и размышлений о глаголах. Просто вызов.

---

### Почему RPC до сих пор жив

Потому что **быстро, просто и честно**.  
REST в микросервисах часто избыточен: ты не работаешь с “ресурсами”, а просто дергаешь методы другого сервиса.  
RPC в таких случаях ближе по смыслу.

---

### Преимущества

1. **Минимум церемоний** — нет урлов, вербов, кодов ответов. Просто вызов.
    
2. **Прямое отображение бизнес-логики** — метод `notifySubscribers()` выглядит логичнее, чем `POST /notifications/subscribers/actions/notify`.
    
3. **Подходит для внутренних API** — между микросервисами, где важна скорость, а не красивая публичная документация.
    
4. **Есть стандарты (JSON-RPC, gRPC)** — можно автогенерировать клиенты.
    

---

### Недостатки

1. **Плохо документируется** — без описания методов API не понять.
    
2. **Сложнее отлаживать через браузер / curl** — всё спрятано в теле запроса.
    
3. **HTTP становится просто транспортом**, теряется нативная семантика `GET/POST/DELETE`.
    
4. **Кэширование и прокси не работают** — всё POST, всё динамическое.
    

---

### Когда использовать RPC

✅ Между микросервисами внутри одной инфраструктуры  
✅ Когда API — это набор команд, а не сущностей  
✅ Когда важнее скорость и простота, чем “чистота архитектуры”

🚫 Не использовать для публичных API, интеграций и фронтов — REST/GraphQL тут лучше.

---

### Пример реального RPC-вызова в PHP

```php
// Клиент
$response = file_get_contents('http://api.local/jsonrpc', false, stream_context_create([
    'http' => [
        'method'  => 'POST',
        'header'  => 'Content-Type: application/json',
        'content' => json_encode([
            'jsonrpc' => '2.0',
            'method'  => 'sum',
            'params'  => [10, 5],
            'id'      => 1
        ])
    ]
]));

$result = json_decode($response, true);
echo $result['result']; // 15
```

---

### RPC против REST

|Сравнение|RPC|REST|
|---|---|---|
|Основная единица|Метод|Ресурс|
|Эндпоинты|`/method`|`/resource`|
|Семантика|"Что сделать"|"Что изменить"|
|Протокол|Любой (HTTP, TCP, AMQP)|HTTP|
|Кэширование|Нет|Да|
|Простота|Прямой вызов|Нужна структура|
|Лучшее применение|Межсервисное общение|Публичное API|

---

### Итог

RPC — это старый, надёжный и до смешного прямолинейный подход.  
Он не притворяется “архитектурным стилем”, не рассуждает про “ресурсы” — просто **вызывает функцию через сеть**.

Если REST — это дипломат,  
GraphQL — это переговорщик,  
то RPC — это парень из техподдержки, который просто берёт трубку и делает работу без разговоров.

