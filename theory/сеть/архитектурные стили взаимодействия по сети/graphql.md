### Что это

GraphQL — **язык запросов к API**, придуманный в Facebook в 2012 году.  
Не фреймворк, не база, не альтернатива REST, а просто **способ общаться с сервером** через один эндпоинт.

Клиент сам описывает, **какие данные ему нужны**, а сервер возвращает их в точности — без мусора и без лишних запросов.

---

### Ключевая идея

В REST у тебя есть куча эндпоинтов:

```
/users/42
/users/42/posts
/users/42/friends
```

В GraphQL один:

```
POST /graphql
```

и ты говоришь серверу, **что именно хочешь получить**:

```graphql
query {
  user(id: 42) {
    name
    email
    posts(limit: 2) {
      title
      likes
    }
  }
}
```

Сервер вернёт **ровно то, что попросил**:

```json
{
  "data": {
    "user": {
      "name": "Mikhail Savin",
      "email": "mikhail.d.savin@gmail.com",
      "posts": [
        { "title": "Why PHP still matters", "likes": 50 },
        { "title": "Symfony internals", "likes": 32 }
      ]
    }
  }
}
```

---

### Основные типы операций

|Тип|Описание|Аналог в REST|
|---|---|---|
|**query**|Получение данных|`GET`|
|**mutation**|Изменение/создание/удаление|`POST` / `PUT` / `DELETE`|
|**subscription**|Реальное время (через WebSocket)|WebSocket / SSE|

---

### Схема

GraphQL API описывается **схемой** — декларативной типизацией данных.

```graphql
type User {
  id: ID!
  name: String!
  email: String!
  posts(limit: Int): [Post!]!
}

type Post {
  id: ID!
  title: String!
  content: String
  likes: Int!
}

type Query {
  user(id: ID!): User
}
```

Эта схема — контракт между клиентом и сервером.  
Из неё можно сгенерировать типы для фронта (TypeScript, Swift, Kotlin) и для бэка (PHP, Go, Node).

---

### Почему GraphQL появился

REST хорош, пока данные простые.  
Но реальность — ад из взаимосвязанных сущностей: пользователь → заказы → товары → отзывы → метки.

REST в таких случаях делает 5 запросов подряд.  
GraphQL делает **один**, комбинируя данные через дерево запросов.

---

### Преимущества

1. **Меньше запросов**  
    Клиент формирует один запрос вместо нескольких REST-запросов.
    
2. **Нет overfetching/underfetching**  
    Получаешь ровно то, что нужно, без “лишнего” и без дополнительных походов за недостающими полями.
    
3. **Единая точка входа**  
    Один `/graphql` вместо десятков эндпоинтов.
    
4. **Схема как документация**  
    Типы и поля описаны явно.  
    IDE (GraphiQL, Playground, Altair) умеют автодополнение и тестирование прямо в браузере.
    
5. **Генерация типов**  
    Автоматическая синхронизация типов между клиентом и сервером — меньше ручного дублирования моделей.
    

---

### Недостатки

1. **Сложность на сервере**  
    Нужны резолверы, слои маппинга, контроль N+1 запросов.  
    Без DataLoader’а всё превратится в SQL-фейерверк.
    
2. **Плохое кэширование на уровне HTTP**  
    Все запросы — POST. Кэшировать приходится на уровне приложения.
    
3. **Переусложнение простых API**  
    Если у тебя три эндпоинта — REST проще и быстрее.
    
4. **Неочевидные уязвимости**  
    Клиенты могут строить слишком “глубокие” запросы, вызывая десятки внутренних резолверов.  
    Решается лимитами (`maxDepth`, `maxComplexity`).
    

---

### Пример мутации

```graphql
mutation {
  createPost(input: {
    title: "New post about Symfony",
    content: "It’s all about EventDispatcher"
  }) {
    id
    title
  }
}
```

Ответ:

```json
{
  "data": {
    "createPost": {
      "id": "55",
      "title": "New post about Symfony"
    }
  }
}
```

---

### Пример подписки (real-time)

```graphql
subscription {
  newComment(postId: 55) {
    id
    text
    author { name }
  }
}
```

Фронт подключается по WebSocket и получает новые комментарии в реальном времени.  
REST так не умеет без костылей.

---

### Когда использовать GraphQL

✅ У тебя богатый фронт (SPA, React/Vue/Svelte)  
✅ Нужно объединить несколько микросервисов за единым API  
✅ Клиенту важен контроль над структурой данных  
✅ Хочешь автогенерацию типов и self-documented API

🚫 Не стоит — если API простое и редко меняется. REST быстрее, дешевле, понятнее.

---

### Инструменты

|Для PHP|Для фронта|
|---|---|
|[webonyx/graphql-php](https://github.com/webonyx/graphql-php) — низкоуровневый сервер|GraphiQL / Altair / Apollo Client|
|[OverblogGraphQLBundle](https://github.com/overblog/GraphQLBundle) — Symfony-интеграция|Relay (от Meta)|
|[Lighthouse](https://lighthouse-php.com/) — Laravel сервер|urql, Apollo|

---

### Итого

GraphQL — это **гибкий слой между клиентом и твоими сервисами**.  
Он не отменяет REST, но делает его умнее:  
вместо десятка “ресурсов” ты получаешь одно умное дерево данных, описанное типами.

REST говорит:

> “Вот пять разных урлов, собери сам.”  
> GraphQL говорит:  
> “Опиши, что хочешь, я соберу за тебя.”

