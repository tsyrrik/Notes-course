**S3 (Simple Storage Service)** — изначально облачный сервис от AWS (2006),  
но со временем стал **открытым индустриальным стандартом для объектных хранилищ**.

Сейчас **“S3” — это не просто продукт Amazon**, а **API-протокол**,  
который поддерживают десятки self-hosted и облачных решений.

---

### 1. Протокол

S3 описывает **HTTP-интерфейс для объектного хранения**:

- **REST API** (GET, PUT, DELETE, LIST, HEAD)
    
- Объекты адресуются как:
    
    ```
    https://{bucket}.s3.{region}.amazonaws.com/{key}
    ```
    
- Формат ответов, заголовков (`x-amz-*`) и подписи запросов (Signature V4)  
    стал стандартом де-факто, и его поддерживают сторонние хранилища.
    

---

### 2. Совместимые реализации (S3-compatible)

|Название|Тип|Комментарий|
|---|---|---|
|**MinIO**|Self-hosted|Самая популярная open-source альтернатива, совместимая с AWS SDK|
|**Ceph (RGW)**|Self-hosted / Enterprise|Объектное хранилище с S3 API|
|**Wasabi**|Облако|Полностью совместимо с AWS SDK, дешевле AWS|
|**DigitalOcean Spaces**|Облако|API тот же, только URL другой|
|**Backblaze B2**|Облако|S3-совместимый endpoint|
|**Cloudflare R2**|Облако|S3-совместимый, без платы за egress|

Любой клиент или библиотека, работающая с S3 (например `aws-sdk`),  
будет работать с этими системами при указании своего `endpoint` вместо `amazonaws.com`.

---

### 3. Пример self-hosted S3 (MinIO)

```bash
docker run -p 9000:9000 -p 9001:9001 \
  -e "MINIO_ROOT_USER=admin" \
  -e "MINIO_ROOT_PASSWORD=secret123" \
  quay.io/minio/minio server /data --console-address ":9001"
```

Подключение (PHP, Python, Go и т. д.):

```php
$s3 = new S3Client([
    'endpoint' => 'http://localhost:9000',
    'use_path_style_endpoint' => true,
    'region' => 'us-east-1',
    'credentials' => [
        'key'    => 'admin',
        'secret' => 'secret123',
    ],
]);
```

---

### 4. Что даёт использование протокола S3

- 🔄 **Совместимость:** можно менять провайдера без переписывания кода.
    
- 🧩 **Лёгкий self-hosting:** MinIO или Ceph без завязки на AWS.
    
- 🧠 **Простая интеграция:** SDK для всех языков уже “умеют” в S3.
    
- 💰 **Контроль затрат:** можно выбирать между AWS, R2, Wasabi, локальным MinIO и т. д.

### Связанные заметки

- [[CDN (Content Delivery Network)]] — CDN часто используют S3-совместимое хранилище как origin.
- [[Стратегии кеширования (Cache Strategies)]] — когда CDN или приложения кэшируют объекты, лежащие в S3.
- [[Прогрев кеша]] — помогает заранее прогреть статику после загрузки в бакет.



