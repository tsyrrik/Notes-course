### Что это

**SSE (Server-Sent Events)** — это механизм, который позволяет серверу **постоянно отправлять данные клиенту**, используя обычное **HTTP-соединение**.  
Односторонняя альтернатива WebSocket:  
сервер → клиент, но не наоборот.

То есть:

- клиент открывает запрос,
    
- сервер **не закрывает соединение**,
    
- время от времени шлёт строки в виде событий.
    

---

### Пример работы

Клиент (JS):

```js
const sse = new EventSource('/events');

sse.onmessage = (event) => {
  console.log('Новое сообщение:', event.data);
};

sse.addEventListener('alert', e => {
  console.log('Аларм:', e.data);
});
```

Сервер (PHP):

```php
header('Content-Type: text/event-stream');
header('Cache-Control: no-cache');

while (true) {
    echo "event: alert\n";
    echo 'data: ' . json_encode(['msg' => 'Сервер жив']) . "\n\n";
    ob_flush();
    flush();
    sleep(5);
}
```

Браузер держит соединение открытым и выводит новые события по мере поступления.  
Если соединение рвётся, клиент сам попытается переподключиться.

---

### Как это выглядит в DevTools

Вкладка **Network → EventStream** (или “Other”):

```
:ok
event: message
data: {"text": "новое уведомление"}
event: alert
data: {"type": "warning", "time": "15:24"}
```

Браузер не закрывает запрос, длина тела = ∞.

---

### Формат протокола

SSE — это обычный текстовый поток, где каждая “порция” отделена пустой строкой:

```
id: 15
event: message
data: {"user":"Mike","text":"Hi!"}

event: update
data: {"counter":42}
```

HTTP-заголовок:  
`Content-Type: text/event-stream`

Клиент умеет:

- восстанавливать соединение (`Last-Event-ID`)
    
- автоматически переподключаться (`retry:`)
    

---

### Почему SSE проще WebSocket

|Фактор|SSE|WebSocket|
|---|---|---|
|Направление|сервер → клиент|двусторонний|
|Протокол|обычный HTTP|отдельный|
|Поддержка браузеров|везде (кроме IE)|тоже везде|
|Шифрование|HTTPS|WSS|
|Балансировка|стандартная|требует sticky sessions|
|Реализация в PHP|элементарна|нужна отдельная служба|
|Использование CDN|да|почти нет|
|Формат|текст|бинарный|

SSE — **HTTP до конца**, не требует апгрейда соединения (`Upgrade: websocket`).  
Браузеры и прокси с ним дружат: можно пустить через nginx, cloudflare и всё остальное без спецнастроек.

---

### Где SSE лучше WebSocket

✅ **Уведомления, метрики, прогресс-бары**  
— когда нужно только “сообщить клиенту”.

✅ **Мониторинги, стримы логов, обновления страниц без перезагрузки.**  
— идеален для push-событий.

✅ **Простота деплоя.**  
— работает в том же PHP-контейнере, без отдельного демона.

✅ **Инфраструктура остаётся стандартной.**  
— HTTP/1.1 и HTTP/2 без хитростей, nginx/Apache проксируют без проблем.

---

### Где WebSocket всё-таки нужен

🚫 Когда требуется **двусторонняя коммуникация** (чат, игровой сервер).  
🚫 Когда клиент должен **посылать команды** в реальном времени.  
🚫 Когда частота сообщений высокая (SSE ≈ 5–10 msg/sec комфортно, дальше душит TCP-буфер).

---

### SSE в PHP-инфраструктуре

Можно реализовать прямо на фреймворке:

```php
// Symfony / Laravel контроллер
return response()->stream(function () {
    while (true) {
        echo "data: " . json_encode(getLatestNotifications()) . "\n\n";
        ob_flush(); flush();
        sleep(2);
    }
}, 200, ['Content-Type' => 'text/event-stream']);
```

Nginx конфиг:

```nginx
location /events {
    proxy_pass http://php-fpm:9000;
    proxy_buffering off;
}
```

Важно: `proxy_buffering off;` — иначе nginx буферизует поток и клиент ничего не увидит.

---

### Symfony Mercure

Mercure — это готовая реализация SSE-хаба.  
Её придумали в экосистеме Symfony, но можно юзать отдельно.

**Схема:**

```
PHP → Mercure Hub → Browser (EventSource)
```

PHP публикует события:

```php
$hub->publish(new Update('/notifications', json_encode($data)));
```

Mercure рассылает их подписчикам:

```js
new EventSource('https://hub/.well-known/mercure?topic=/notifications')
```

SSE-уровень полностью берёт на себя Mercure — персистентность, reconnect, ACL, токены.

---

### Преимущества SSE в реальных проектах

- **Никаких сторонних сервисов** — PHP сам может стримить.
    
- **Работает через HTTPS и HTTP/2** (легко деплоится).
    
- **Масштабируется через обычные прокси** — nginx, Traefik, HAProxy.
    
- **Прозрачно для фронта** — `EventSource` встроен во все браузеры.
    
- **Упрощённая модель безопасности** — всё на токенах JWT или cookie, не нужно WS-хэндшейков.
    

---

### Минусы (честно)

- Односторонний: клиент не может писать на сервер.
    
- TCP-сессии висят открытыми (надо следить за количеством).
    
- PHP-воркеры нужно запускать через long-running runtime (Swoole, RoadRunner) или отдавать через отдельный поток (иначе FPM умрёт).
    
- IE не поддерживает (но если вы всё ещё поддерживаете IE — вы заслужили это).
    
