**Генераторы** — это синтаксический сахар для создания **итераторов**.  
Они позволяют возвращать данные “поштучно”, без явного создания классов, реализующих `Iterator`.

---

### 1. Что это вообще такое

Генератор — это функция, которая **“запоминает” своё состояние** между вызовами  
и возвращает значения через `yield`, а не `return`.

```php
function counter(int $max): Generator {
    for ($i = 1; $i <= $max; $i++) {
        yield $i;
    }
}

foreach (counter(3) as $num) {
    echo $num; // 123
}
```

💡 Важно: `yield` — это не “магическая оптимизация по памяти”.  
PHP всё равно хранит внутренний контекст функции, просто не строит заранее массив.  
Генераторы не делают код “в 100 раз быстрее”,  
они лишь **откладывают создание значений**, пока они реально не понадобятся.

---

### 2. Что происходит под капотом

Когда функция с `yield` вызывается:

- PHP **не выполняет её тело сразу**;
    
- создаётся объект класса `Generator`;
    
- при каждом `foreach`-шаге вызывается `next()`, и выполнение продолжается до следующего `yield`.
    

Это примерно то же, как если бы ты написал свой итератор:

```php
class Counter implements Iterator {
    public function __construct(private int $max, private int $i = 1) {}
    public function current(): int { return $this->i; }
    public function key(): int { return $this->i - 1; }
    public function next(): void { $this->i++; }
    public function rewind(): void { $this->i = 1; }
    public function valid(): bool { return $this->i <= $this->max; }
}
```

…только с `yield` всё в одну строку.

---

### 3. Пример с ключами и возвратом значения

```php
function squares(): Generator {
    for ($i = 1; $i <= 3; $i++) {
        yield "n=$i" => $i * $i;
    }
    return "done";
}

$gen = squares();

foreach ($gen as $key => $val) {
    echo "$key => $val\n";
}
echo $gen->getReturn(); // done
```

🧠 `yield` может возвращать не только значение, но и **ключ**.  
После завершения генерации можно получить результат через `getReturn()`.

---

### 4. Отправка данных в генератор

```php
function printer(): Generator {
    while (true) {
        $msg = yield;
        echo "Got: $msg\n";
    }
}

$g = printer();
$g->send('hello'); // Got: hello
$g->send('world'); // Got: world
```

> `send()` — это двусторонняя связь: можно “вкидывать” данные обратно в генератор.  
> Используется в корутинах и асинхронных реализациях.

---

### 5. `yield from` — делегирование итерации

Если одна функция хочет “уступить” управление другой — можно использовать `yield from`:

```php
function sub() {
    yield 1;
    yield 2;
}
function main() {
    yield 0;
    yield from sub();
    yield 3;
}

foreach (main() as $v) echo $v; // 0 1 2 3
```

> `yield from` позволяет “вклинить” другой генератор или массив как часть потока.  
> Это эквивалент ручного `foreach (sub() as $v) yield $v;`.

---

### 6. Ленивые итераторы

Генераторы позволяют делать **ленивые вычисления** — значения создаются _по требованию_,  
а не заранее. Это особенно полезно при обработке больших файлов, потоков, API-результатов.

```php
function readLines(string $path): Generator {
    $handle = fopen($path, 'r');
    while (($line = fgets($handle)) !== false) {
        yield trim($line);
    }
    fclose($handle);
}
```

> Это не “меньше памяти” в абсолюте,  
> а **меньше пиковая нагрузка** — мы не держим в памяти весь файл сразу.

---

### 7. Rewindable vs Non-Rewindable Generators

- **Обычные генераторы** в PHP **не rewindable**.  
    То есть нельзя пройтись по ним второй раз — `rewind()` не работает после `valid() == false`.
    

```php
$g = counter(3);
foreach ($g as $n) echo $n; // 123
foreach ($g as $n) echo $n; // ничего не выведет
```

- Если нужно “перематываемое” поведение,  
    просто возвращай новый генератор из функции:
    
    ```php
    function getNumbers(): Generator { yield from [1,2,3]; }
    foreach (getNumbers() as $x) {} // каждый вызов — новый итератор
    ```
    

---

### 8. Когда использовать генераторы

✅ Когда нужно обходить большие наборы данных (файлы, API)  
✅ Когда хочется лаконично описать итерацию без лишнего класса  
✅ Когда важно ленивое исполнение  
🚫 Не для микропроизводительности — прирост от “yield” часто нейтрален

---

### 💡 В одной фразе

> **Генератор** — это ленивый итератор, написанный одной функцией.  
> Он не экономит память магически,  
> но делает код чище, итерацию понятнее и работает “по требованию”.

[[Файберы (Fibers) в PHP]]
