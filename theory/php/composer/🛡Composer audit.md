
### Что делает

Команда сравнивает твои зависимости с базой известных уязвимостей.

Источники:

- **FriendsOfPHP/security-advisories** (де-факто стандарт для PHP)  
    [https://github.com/FriendsOfPHP/security-advisories](https://github.com/FriendsOfPHP/security-advisories)
    
- Дополнительно Composer может использовать **OSV.dev** и NVD (через прокси Packagist)
    
- Уязвимости описываются в формате **advisories**, привязаны к конкретным диапазонам версий
    

Composer скачивает JSON с GitHub и проверяет твой `composer.lock`  
против списка уязвимых версий.

### Пример вывода

```bash
$ composer audit

Found 1 security vulnerability in your dependencies:

symfony/http-foundation
------------------------
 * CVE-2023-12345: Response header injection in file downloads
   Affected versions: >=4.0 <4.4.50
   Fixed in: 4.4.50

1 packages have known vulnerabilities.
```

Если всё чисто:

```bash
No security vulnerability advisories found.
```

### Рекомендация

Добавляй в CI:

```bash
composer audit --format=json
```

Если есть уязвимости ― падай билд.

---

## 📚 Откуда берутся уязвимости (в деталях)

### База FriendsOfPHP:

- Это curated-репозиторий advisories для PHP-пакетов
    
- Формат — YAML с описанием: CVE, ссылка, диапазон версий, тип проблемы
    
- Packagist и Composer автоматически синхронизируют эту базу
    
- Обновляется мгновенно при merge PR от maintainers/security researchers
    

Пример advisory:

```yaml
reference: CVE-2022-21706
title: Remote code execution vulnerability
link: https://github.com/vendor/package/security/advisories/GHSA-xxxx
cve: CVE-2022-21706
branches:
  "1.x":
    versions: "<1.5.3"
  "2.x":
    versions: "<2.1.1"
```

Composer понимает: версии ниже `1.5.3` и `2.1.1` небезопасны.

### OSV.dev (от Google / OpenSSF)

- Универсальная база уязвимостей для всех экосистем: npm, PyPI, Packagist
    
- Composer умеет использовать OSV как fallback
    
- Всё унифицировано по CVE/CWE
    

### NVD (National Vulnerability Database)

- Данные поступают через Packagist
    
- CVE влияет на вывод `composer audit`, если advisory привязан к пакету
    

---

## 🧩 Практическая польза

Эти команды позволяют:

- вовремя находить дыры безопасности
    
- видеть, когда зависимости сильно устарели
    
- убедиться, что код будет работать на целевой платформе
    
- гарантировать корректность composer.json
    
- встроить безопасность в CI/CD
    

---

## 💡 В одной фразе

> `composer audit` и `composer outdated` ― это твой маленький SAST.  
> Автоматизируй их в CI и забудь про ручное выискивание уязвимостей и разбросанных обновлений.