## 📚 Оглавление
1. [Что делает Composer](#-что-делает-composer)
2. [Основные файлы](#-основные-файлы)
3. [Когда нужен composerlock](#-когда-нужен-composerlock)
4. [Версионирование и semver](#-версионирование-и-semver)
5. [Зависимости по ветке-и-коммиту](#-зависимости-по-ветке-и-коммиту)
6. [Типы пакетов](#-типы-пакетов)
7. [Репозитории](#-репозитории)
8. [Авторизация authjson](#-авторизация-authjson)
9. [Проверки-и-аудит](#-проверки-и-аудит)
10. [Автолоад-и-оптимизация](#-автолоад-и-оптимизация)
11. [Prod-и-dev-зависимости](#-prod-и-dev-зависимости)
12. [Зависимости-от-php-и-расширений](#-зависимости-от-php-и-расширений)
13. [configplatform](#-configplatform)
14. [Полезные-команды](#-полезные-команды)
15. [Команды-композитора-и-шорткаты](#-команды-композитора-и-шорткаты)
16. [Ссылки](#-ссылки)

---

## 🧠 Что делает Composer
Composer управляет зависимостями PHP-проекта:  
решает, **какие версии библиотек совместимы**, скачивает их и готовит автозагрузку (`vendor/autoload.php`).

---

## 📦 Основные файлы

| Файл | Назначение |
|------|-------------|
| `composer.json` | описание проекта, зависимостей и настроек |
| `composer.lock` | зафиксированные версии (гарантирует одинаковую сборку) |
| `vendor/` | установленные пакеты и автолоад |

---

## 🔒 Когда нужен `composer.lock`

| Контекст | Нужен ли lock |
|-----------|----------------|
| **Приложение** (API, сервис) | ✅ Да — фиксирует версии для CI и продакшена |
| **Библиотека** (на Packagist) | ❌ Нет — lock не используется при установке |

> **Правило:** если проект что-то *запускает*, lock обязателен.

---

## 🧮 Версионирование и SemVer

Composer использует [**Semantic Versioning**](https://semver.org/lang/ru/)  
Формат: `MAJOR.MINOR.PATCH` → `2.3.4`

| Пример | Что значит |
|---------|-------------|
| `^2.3` | `>=2.3.0 <3.0.0` |
| `~2.3` | `>=2.3.0 <2.4.0` |
| `>=1.0 <2.0` | диапазон |
| `2.*` | всё между 2.0 и 3.0 |
| `dev-main` | последняя версия ветки main |
| `dev-master#abcd1234` | конкретный коммит |

---

## 🌿 Зависимости по ветке и коммиту

```bash
composer require vendor/package:dev-feature-branch
composer require vendor/package:dev-master#abcd123
````

Если Composer ругается на нестабильную версию — добавь `@dev`:

```json
"require": {
  "vendor/package": "dev-feature-branch@dev"
}
```

⚠️ Для продакшена такие зависимости фиксируй в lock или замени на тег.

---

## 🧩 Типы пакетов

|type|Назначение|
|---|---|
|`library`|обычная библиотека|
|`project`|приложение (создаёт lock, может содержать скрипты деплоя)|
|`composer-plugin`|расширяет сам Composer|
|`metapackage`|“пустышка”, объединяющая зависимости|
|`symfony-bundle`, `phpstan-extension`, `wordpress-plugin`|специальные типы для экосистем|

---

## 🌐 Репозитории

По умолчанию Composer ищет пакеты на [Packagist.org](https://packagist.org/).  
Но можно подключить свои источники:

```json
"repositories": [
  { "type": "vcs", "url": "https://github.com/msavin/private-package" },
  { "type": "path", "url": "../local-package", "options": { "symlink": true } },
  { "type": "composer", "url": "https://packages.company.local" }
]
```

Типы:

- `vcs` — git-репозиторий (GitHub, GitLab и др.)
    
- `path` — локальный каталог (удобно при разработке нескольких пакетов)
    
- `composer` — свой packagist-сервер
    
- `artifact` — директория с `.zip`-архивами пакетов
    

---

## 🔐 Авторизация (`auth.json`)

Для приватных репозиториев:

```json
{
  "github-oauth": { "github.com": "ghp_xxx" },
  "gitlab-token": { "gitlab.company.local": "glpat-xxx" }
}
```

Пути:

- глобально: `~/.composer/auth.json`
    
- локально: `./auth.json`
    

> ⚠️ Никогда не коммить `auth.json` — добавь в `.gitignore`.

---

## 🧯 Проверки и аудит

[[🛡Composer audit]]
[[Проверки композера]]


---

## ⚙️ Автолоад и оптимизация

[[⚙️ Composer Autoload и оптимизация]]

---

## 🚀 Prod и Dev зависимости

```json
"require": {
  "php": "^8.2",
  "symfony/http-kernel": "^7.0"
},
"require-dev": {
  "phpunit/phpunit": "^11",
  "rector/rector": "^1.2"
}
```

- `--no-dev` отключает установку dev-пакетов.
    
- Типичный прод-билд:
    
    ```bash
    composer install --no-dev --optimize-autoloader --no-interaction --no-progress
    ```
    

---

## 🔌 Зависимости от PHP и расширений

Можно указывать не только PHP-версию, но и конкретные расширения:

```json
"require": {
  "php": "^8.3",
  "ext-json": "*",
  "ext-pdo": "*",
  "ext-gd": "*"
}
```

Проверить соответствие окружения:

```bash
composer check-platform-reqs
```

---

## 🧮 config:platform

Эмулирует другую платформу (например, PHP 8.1 в CI):

```json
"config": {
  "platform": {
    "php": "8.1.0",
    "ext-sodium": "1.0.25",
    "composer-runtime-api": "2.6.0"
  }
}
```

Полезно, чтобы Composer не подбирал зависимости под твою локальную версию PHP.

---

## 🧰 Полезные команды

|Команда|Описание|
|---|---|
|`composer info`|список пакетов|
|`composer show vendor/package`|подробности о пакете|
|`composer why vendor/package`|кто требует пакет|
|`composer licenses`|лицензии|
|`composer check-platform-reqs`|сверяет PHP и расширения|
|`composer fund`|список пакетов, просящих донаты|

---

## ⚡ Команды

Composer использует **Symfony Console**, значит:

- можно писать **неполные команды**, если они однозначны:
    
    ```bash
    composer u     # composer update
    composer i     # composer install
    composer d:a   # composer dump-autoload
    composer a     # composer audit
    ```
    

**Полезные дополнения:**

| Команда                           | Назначение                                                                             |
| --------------------------------- | -------------------------------------------------------------------------------------- |
| `composer bump`                   | автоматическое обновление версии пакета по SemVer                                      |
| `composer update --lock`          | обновляет только lock-файл, не трогая зависимости в `vendor/` (удобно при CI или кэше) |
| `composer outdated --direct`      | проверка прямых зависимостей                                                           |


---

## 🔗 Ссылки

- [SemVer (официальная спецификация)](https://semver.org/lang/ru/)
    
- [Composer schema reference](https://getcomposer.org/doc/04-schema.md)
    
- [Composer config reference](https://getcomposer.org/doc/06-config.md)
    
- [Security advisories database](https://github.com/FriendsOfPHP/security-advisories)

- [[🧱 best practices]]
