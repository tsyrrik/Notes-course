### Что это

**RoadRunner (RR)** — это **высокопроизводительный PHP-рантайм**, написанный на **Go**, который заменяет связку **nginx + php-fpm**.  
Он держит **постоянно живущие PHP-воркеры**, обрабатывает HTTP, gRPC, WebSocket, очереди, задачи — и делает это **без перезапуска интерпретатора при каждом запросе**.

Если коротко — RoadRunner делает для PHP то, что Node.js делает для JavaScript:  
код живёт в памяти, не стартует заново на каждый запрос.

---

### Почему это вообще появилось

Классическая связка:

```
nginx → php-fpm → PHP запускается → выполняет → умирает
```

Каждый запрос пересоздаёт окружение, загружает классы, конфиг, контейнер, коннекты.  
Это медленно и расточительно.

RoadRunner держит воркеры живыми:

```
RR (на Go) → пул PHP-воркеров (долгоживущих)
```

HTTP-запрос приходит в Go, RR выбирает свободного воркера,  
отдаёт ему задачу через socket, получает ответ — и тот воркер остаётся жить для следующего запроса.

---

### Архитектура (простая схема)

```
[Браузер]
   ↓
[RoadRunner HTTP server (Go)]
   ↓
[Pool of PHP workers]
   ├─ index.php (Symfony/Laravel bootstrap)
   ├─ сервисы, контейнер, ORM, DI и всё остальное
   ↓
[Ответ обратно клиенту]
```

- Go-часть — как nginx: принимает запросы, маршрутизирует, балансирует.
    
- PHP-часть — обычный скрипт, но живёт долго и не пересоздаётся.
    
- Воркеры общаются с Go через IPC (stdin/stdout).
    

---

### Основные возможности

- HTTP/1.1 и HTTP/2 сервер
    
- gRPC сервер
    
- WebSocket (через плагин)
    
- Job queues (Kafka, RabbitMQ, Redis, SQS)
    
- Background jobs и pipeline workers
    
- Модульная архитектура — включаешь только нужные плагины
    
- Горячая перезагрузка воркеров при деплое
    

---

### Плюсы

1. **Скорость**
    
    - нет повторного старта PHP-интерпретатора
        
    - код и контейнер остаются в памяти
        
    - 2–5x быстрее, чем php-fpm на типичных API
        
    - очень низкий latency (Go обрабатывает сеть быстрее nginx)
        
2. **Горячие воркеры**  
    DI-контейнер Symfony/Laravel поднимается один раз.  
    ORM-пулы и Redis-соединения можно держать между запросами.
    
3. **Единая точка**  
    RR заменяет nginx, php-fpm, supervisor, и даже часть очередей.
    
4. **Асинхронность**  
    Есть плагины для async-job’ов, WebSocket’ов, gRPC, Kafka и прочего.
    
5. **Нормальная интеграция**  
    Laravel — через `spiral/roadrunner-laravel`  
    Symfony — через `baldinof/roadrunner-bundle`
    

---

### Минусы

1. **Долгоживущий state**
    
    - нужно следить за “грязным” состоянием между запросами: глобальные переменные, singletons, static-кеши.
        
    - всё, что ты сохраняешь, живёт до смерти воркера.
        
2. **Не дружит с некоторыми расширениями**
    
    - всё, что не умеет освобождать память корректно (libxml, GD, mbstring), может течь.
        
3. **Нужна новая логика деплоя**
    
    - `systemctl restart php-fpm` не работает, теперь ты перезапускаешь RR.
        
    - при hot-reload нужно уметь очищать кеши контейнера и сервисов.
        
4. **Не для shared-хостингов и дешёвых окружений**
    
    - требует root/доступ к бинарнику RR, сервисного управления.
        

---

### Когда RoadRunner реально уместен

✅ Высоконагруженные API (REST/GraphQL)  
✅ Микросервисы с gRPC и очередями  
✅ WebSocket-инфраструктура  
✅ Laravel/Symfony-приложения, где latency важен  
✅ Замена php-fpm в Docker/Cloud-среде

🚫 Не нужен — для обычных лендингов, блога или CRM, где запросов немного.

---

### Интеграция с Laravel

Устанавливается пакет:

```bash
composer require spiral/roadrunner-laravel
```

Затем конфиг:

```bash
./vendor/bin/rr get-binary
./rr serve
```

Laravel поднимается один раз, контейнер живёт между запросами.  
Те, кто юзает Horizon или Octane, — это фактически тот же подход, но Octane поверх RR или Swoole.

---

### Интеграция с Symfony

Пакет:

```bash
composer require baldinof/roadrunner-bundle
```

Запуск:

```bash
./rr serve
```

Symfony boot’ится один раз, дальше RR гоняет Request/Response через `HttpKernelInterface`.  
То есть `Kernel::handle()` вызывается многократно без пересоздания контейнера.

---

### Производительность (грубая реальность)

|Среда|1000 req/s|latency|
|---|---|---|
|nginx + php-fpm|~800–1000|~8–12 ms|
|RoadRunner|~3500–4000|~2–3 ms|

При длительных API с JSON — RR реально быстрее и потребляет меньше CPU (примерно вдвое).

---

### Итого

**RoadRunner** — это **Go-сервер и PHP-воркер-пул**,  
который превращает PHP из “скрипта на каждый запрос” в “долгоживущий сервис”.

Плюсы — скорость, контроль, асинхронность.  
Минусы — стейт, утечки, новый подход к деплою.

[[Оптимизация PHP]]
[[Temporal]]
[[FrankenPHP]]
