### Что это

**FrankenPHP** — это **веб-сервер и PHP-SAPI**, написанный на **Go**.  
Он совмещает **HTTP-сервер**, **PHP-интерпретатор** и **современные фичи**, вроде **async-запросов, worker-модели и WebSocket/SSE поддержки**, в одном бинарнике.

Проще говоря, это **альтернатива nginx + php-fpm**, но с фишками, которых раньше в PHP-мире просто не было.

---

### Почему “Franken”?

Потому что он — смесь всего:  
чуть **php-fpm**, чуть **RoadRunner**, чуть **Caddy**, чуть **Swoole**.  
Название — отсылка к “Франкенштейну”: склеено из разных тел, но ожило и работает.

---

### Основная идея

FrankenPHP использует **Go-HTTP-сервер** (тот же, что в Caddy)  
и запускает **встроенный PHP-интерпретатор** прямо в нём,  
в виде **долгоживущих воркеров**, без FPM и без FastCGI.

```
Браузер
  ↓
[FrankenPHP HTTP сервер]
  ↓
[PHP-воркер внутри того же процесса]
  ↓
[Твоя Symfony/Laravel/… логика]
```

Никакого отдельного php-fpm, никакого unix-socket, всё — единый процесс.  
Запросы приходят напрямую в PHP.

---

### Возможности

- HTTP/1.1 и HTTP/2 сервер
    
- HTTPS, автоматический Let's Encrypt
    
- Поддержка **Symfony**, **Laravel**, **API Platform**
    
- Встроенная поддержка **Worker Mode** (долгоживущие воркеры)
    
- **Background jobs**, **WebSocket**, **SSE (Server-Sent Events)**
    
- Совместимость с **PSR-15/PSR-7 middleware**
    
- Zero-config Docker-image с PHP 8.3+
    
- Встроенный **Prometheus monitoring**
    

---

### Пример запуска Symfony

```bash
composer require dunglas/frankenphp
frankenphp php-server public/
```

или через Docker:

```bash
FROM dunglas/frankenphp
COPY . /app
WORKDIR /app
CMD ["php-server", "public/"]
```

Symfony автоматически адаптируется, если у тебя есть `symfony/runtime`.

---

### Почему стало популярным

1. **Разрабатывает Kévin Dunglas** — тот самый, кто создал **API Platform** и **Symfony HTTP Client**.  
    То есть проект — не случайная поделка, а часть Symfony-экосистемы.
    
2. **Zero-config**: запускаешь один бинарник — и у тебя и сервер, и PHP, и HTTPS.
    
3. **Совместим с современными фреймворками**: Symfony, Laravel, API Platform работают “из коробки”.
    
4. **Поддерживает long-lived процессы**, но без боли Swoole (он C-модуль).
    
5. **Хорош для контейнеров** — меньше движущихся частей, чем nginx+php-fpm.
    

---

### Пример архитектуры

```
[FrankenPHP (Go)]
 ├─ HTTP listener :443
 ├─ TLS termination
 ├─ PHP worker pool (in-process)
 ├─ static file handler
 └─ metrics/exporters
```

- Go слушает порты, управляет TLS, воркерами, логами.
    
- PHP-воркеры запускаются в том же процессе и общаются через Go-binding к Zend API.
    
- Запросы маршрутизируются напрямую в твой `index.php`.
    

---

### Плюсы

1. **Нет php-fpm** — меньше latency, меньше накладных расходов.
    
2. **Асинхронные возможности** — WebSocket, SSE, фоновые задачи прямо из PHP.
    
3. **Простота деплоя** — один бинарник, без nginx, без FastCGI.
    
4. **Совместимость с Symfony** — out-of-the-box.
    
5. **Легко в Docker** — образ весит меньше, чем nginx+php-fpm связка.
    
6. **Меньше context switching** — PHP не пересоздаётся, воркеры живут постоянно.
    

---

### Минусы и ограничения

1. **Молодой проект** — пока не battle-tested на огромных продах.
    
2. **Меньше tooling’а и туториалов**, чем у nginx/php-fpm.
    
3. **Stateful воркеры** — как и в RoadRunner/Swoole, нужно чистить глобалы.
    
4. **Пока не заменяет nginx полностью** в edge-сложных сетях (проксирование, rate-limit, rewrites и т.п.).
    
5. **Меньше изоляции**, т.к. всё в одном бинарнике (Go + PHP).
    

---

### Когда использовать FrankenPHP

✅ Для **Symfony**, **API Platform**, **Laravel API**  
✅ В **Docker/Cloud окружениях**, где хочется “всё в одном контейнере”  
✅ Если хочешь **снизить latency и убрать php-fpm**  
✅ Если хочешь SSE / WebSocket / фоновые задачи без Swoole  
✅ Если нужен простой SSL-ready сервер “из коробки”

🚫 Не стоит — если проект на shared-хостинге или нужна строгая nginx-конфигурация, CDN, reverse-proxy и rewrite-магия.

---

### Производительность

В тестах (по данным автора и независимых замеров):

|Среда|Requests/s|Задержка (ms)|
|---|---|---|
|nginx + php-fpm|~1100|9–12|
|RoadRunner|~3500|3–4|
|Swoole|~4000|2–3|
|**FrankenPHP**|**≈3000–3500**|**3–5**|

То есть близко к RoadRunner, при том, что установка проще.

---

### Итого

**FrankenPHP** — это современный, удобный сервер-рантайм для PHP,  
который убирает php-fpm из цепочки и добавляет long-living воркеров, HTTPS и WebSocket прямо “внутрь”.

- Быстрее, чем php-fpm.
    
- Проще, чем RoadRunner.
    
- Безопаснее, чем Swoole (не требует C-расширений).
    

Его место — **между php-fpm и RoadRunner**:  
достаточно зрелый, чтобы использовать в проде, но без фанатизма — просто **современный, цельный сервер для PHP-приложений**.



[[RoadRunner]]
[[Оптимизация PHP]]
[[Swoole]]
