### Что это

**Swoole** — это **C-расширение для PHP**, которое превращает язык из синхронного в **асинхронный, многопоточный, неблокирующий сервер**.  
Короче: PHP начинает вести себя как Node.js — с event loop, таймерами, сокетами и корутинами.

Он работает **внутри самого PHP**, без nginx, без php-fpm, без Go, без посредников.  
Ты получаешь собственный HTTP-сервер прямо в PHP-коде.

---

### Что делает

- Реализует **HTTP, WebSocket, TCP/UDP** сервера прямо в PHP.
    
- Поддерживает **корутины** (легковесные потоки, похожие на async/await).
    
- Позволяет держать **постоянные соединения** и **кешировать объекты в памяти** между запросами.
    
- Может работать как фреймворк-движок (Laravel Octane, Hyperf, Swoft, OpenSwoole).
    
- Заменяет php-fpm, Supervisor, cron и даже очереди.
    

---

### Пример: минимальный HTTP-сервер

```php
$server = new Swoole\Http\Server("0.0.0.0", 9501);

$server->on("request", function ($req, $res) {
    $res->header("Content-Type", "text/plain");
    $res->end("Hello from Swoole!");
});

$server->start();
```

Без nginx, без php-fpm.  
Просто запускаешь `php server.php`, и PHP начинает слушать порт 9501.

---

### Архитектура

```
Браузер
  ↓
[Swoole HTTP server (C-расширение)]
  ↓
[PHP runtime + event loop + корутины]
  ↓
[Твой код, контейнер, ORM, логика]
```

- Каждый воркер живёт в памяти.
    
- Swoole управляет event loop’ом и пулом процессов.
    
- Внутри можно запускать таймеры, WebSocket, очереди, корутины.
    

---

### Преимущества

1. **Скорость**
    
    - Нет php-fpm.
        
    - Нет перезапуска интерпретатора.
        
    - Асинхронный I/O — запросы, сокеты, файлы.
        
    - Производительность сравнима с Node.js и Go.
        
2. **Постоянное состояние**
    
    - Можно держать коннекты к базе, Redis, DI-контейнер, кэш — всё живёт между запросами.
        
3. **WebSocket из коробки**
    
    - Прямо внутри PHP без стороннего брокера.
        
4. **Корутины**
    
    - `go(function() { ... })` создаёт лёгкий параллельный поток, не блокируя другие.
        
5. **Полноценный сервер**
    
    - HTTP, WebSocket, gRPC, Task Workers, Timer — всё встроено.
        

---

### Минусы

1. **Опасность состояния**
    
    - Всё живёт между запросами. Забыл очистить глобал — привет утечка и баги.
        
    - Фреймворки вроде Laravel/Symfony не готовы к этому из коробки.
        
2. **Не работает на shared-хостингах**
    
    - Нужно ставить расширение Swoole, запускать процесс, держать порты.
        
        
3. **Утечки памяти**
    
    - Если не обнулять ресурсы (PDO, curl, и т.п.), воркер постепенно пухнет.
        
4. **Не все расширения PHP потокобезопасны**
    

---

### Когда Swoole реально нужен

✅ Real-time приложения (чаты, WebSocket, игры, торги)  
✅ Высоконагруженные API и очереди  
✅ Асинхронные задачи: рассылка, стриминг, websocket-push  
✅ Laravel Octane (один из движков под капотом)  
✅ Если хочешь “живой” PHP-сервер без nginx

🚫 Не нужен — если у тебя REST-бекенд на 100 rps и фреймворк без адаптации под long-living процессы.

---

### Интеграция с фреймворками

#### Laravel — через **Octane**

```bash
composer require laravel/octane
php artisan octane:start --server=swoole
```

Laravel Octane умеет держать приложение в памяти,  
обновлять контейнер между запросами и работать с Swoole или RoadRunner.

#### Symfony

Symfony напрямую не поддерживает Swoole, но можно через проекты вроде:

- `k911/swoole-bundle` (старый, но рабочий)
    
- `openswoole/symfony-http-server`
    

---

### Альтернативы и сравнение

|Характеристика|**php-fpm**|**RoadRunner**|**Swoole**|
|---|---|---|---|
|Перезапуск PHP на каждый запрос|✅|❌|❌|
|Асинхронность|❌|частично|✅|
|WebSocket|через внешние сервисы|плагин|из коробки|
|gRPC|нет|плагин|из коробки|
|Требует расширение|нет|нет|✅ (C-модуль)|
|Производительность|средняя|высокая|очень высокая|
|Память между запросами|нет|да|да|
|Поддержка Laravel/Symfony|частичная|хорошая|через Octane / bundle|

---


### Итого

**Swoole** — это “PHP без тормозов”:  
быстрый, асинхронный, способный держать миллион соединений,  
но требующий дисциплины и понимания, что “глобалы не исчезают”.

Если **RoadRunner** — это php-fpm на стероидах,  
то **Swoole** — это **Node.js на PHP**: без посредников, без nginx, прямо в твоём коде.



[[Оптимизация PHP]]
[[FrankenPHP]]
