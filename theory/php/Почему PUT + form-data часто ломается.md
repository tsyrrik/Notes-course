Когда ты делаешь `PUT`-запрос с типом `multipart/form-data` (т.е. с файлами),  
многие веб-сервера, прокси и фреймворки **не умеют корректно парсить тело** такого запроса.

Исторически `multipart/form-data` придумали под **POST**,  
и большинство middleware/библиотек (в PHP, Node.js, Python и т.д.)  
распознают multipart только при `POST`.

Например, в Symfony / Laravel / Express:

- `$_FILES` и `$_POST` заполняются **только при POST**;
    
- `PUT` обрабатывается как “raw body” — и ты получаешь неразобранные байты.
    

---

## Обходное решение — `_method=PUT`

Ты можешь отправить **POST-запрос**, но добавить скрытое поле `_method=PUT`:

```
POST /upload
Content-Type: multipart/form-data

_form-data:
  file: <файл>
  title: Example
  _method: PUT
```

Фреймворки вроде Symfony, Laravel, Rails и др.  
автоматически определяют это поле и **преобразуют запрос в PUT** на уровне логики.  
То есть для контроллера это будет “PUT”,  
но по сети — обычный `POST`, который корректно парсится.

---

## Почему это нормально

- HTTP-спецификация не запрещает override метода.
    
- Этот приём стал стандартом де-факто (HTML-формы всё равно умеют только `GET` и `POST`).
    
- Все популярные backend-фреймворки поддерживают `_method`-override нативно.
    

---

## Итог

|Вариант|Что происходит|Проблемы|
|---|---|---|
|`PUT multipart/form-data`|тело не парсится|`$_FILES` пустой|
|`POST multipart/form-data + _method=PUT`|всё парсится корректно|формально “трюк”, но стандартный|

---

### 💡 Резюме

> Чтобы загрузить файл через “PUT”,  
> **отправляй `POST` с `_method=PUT`** —  
> сервер поймёт, что это логически PUT,  
> но данные распарсятся корректно как form-data.