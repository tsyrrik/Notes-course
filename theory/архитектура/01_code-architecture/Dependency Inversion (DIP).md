
Принцип инверсии зависимостей — пятая буква из **SOLID**.  
Он описывает, **как связывать уровни системы**, чтобы код был гибким и расширяемым.

---

### 1. Две идеи в основе

#### **а) Модули верхнего уровня не должны зависеть от модулей нижнего уровня.**

Оба должны зависеть от **абстракций**.

#### **б) Абстракции не должны зависеть от деталей.**

Детали (конкретные реализации) должны зависеть от абстракций.

---

### 2. Что за «уровни»?

**Модуль верхнего уровня** — это тот, кто решает бизнес-задачу.  
**Модуль нижнего уровня** — тот, кто делает техническую работу (отправка HTTP, работа с БД, интеграции и т. п.).

Проблема: если верхний уровень напрямую использует нижний, он **знает слишком много** о том, _как именно_ это работает.  
Из-за этого любое изменение в деталях ломает бизнес-логику.

---

### 3. Пример: разные методы оплаты

#### Без инверсии зависимостей

```php
class CheckoutService
{
    public function pay(Order $order): void
    {
        $paypal = new PaypalPaymentGateway();
        $paypal->send($order->getTotal());
    }
}
```

`CheckoutService` (верхний уровень) зависит от `PaypalPaymentGateway` (нижний уровень).  
Если добавим оплату через Stripe — придется менять CheckoutService. Он жёстко связан с конкретной реализацией.

---

#### С применением Dependency Inversion

```php
interface PaymentGatewayInterface
{
    public function send(float $amount): void;
}

class PaypalPaymentGateway implements PaymentGatewayInterface
{
    public function send(float $amount): void
    {
        // логика PayPal
    }
}

class StripePaymentGateway implements PaymentGatewayInterface
{
    public function send(float $amount): void
    {
        // логика Stripe
    }
}

class CheckoutService
{
    public function __construct(private PaymentGatewayInterface $gateway) {}

    public function pay(Order $order): void
    {
        $this->gateway->send($order->getTotal());
    }
}
```

Теперь `CheckoutService` не зависит от деталей.  
Он просто знает: _у него есть кто-то, кто умеет отправлять оплату_.

Инверсия в том, что **высокоуровневый код диктует интерфейс**,  
а конкретные классы низкого уровня **подстраиваются под него**, а не наоборот.

---

### 4. Почему это важно

- Можно подменить `PaymentGatewayInterface` хоть фейком в тестах, хоть новым провайдером.
    
- Код не знает, как именно выполняется задача, только _что_ она выполняется.
    
- Верхний уровень не «приседает» под нижний — наоборот, нижний адаптируется под требования верхнего.
    

---

### 5. Абстракции и детали — коротко

- **Абстракция**: интерфейс или базовый контракт.
    
- **Деталь**: конкретный класс, реализующий интерфейс.
    
- Инверсия в том, что **деталь зависит от абстракции**, а не наоборот.
    

---

### 💡 В одной фразе

> Dependency Inversion — это когда бизнес-логика диктует интерфейс,  
> а технические детали обязаны ему подчиняться.