**Package by Feature** — это способ организации кода,  
при котором структура проекта строится **по функциональности**,  
а не по техническим слоям.

---

### 1. Проблема [[Package by Layer]]


Выглядит аккуратно, но плохо масштабируется:  
чтобы понять, как работает создание заказа,  
нужно искать по всем слоям — `OrderController`, `OrderService`, `OrderRepository`, `OrderType`, `OrderValidator`...

---

### 2. Что делает “package by feature”

Мы группируем всё вокруг **бизнес-фичи**, а не технической роли:

```
src/
 ├── Orders/
 │   ├── OrderController.php
 │   ├── OrderService.php
 │   ├── OrderRepository.php
 │   └── Entity/
 ├── Billing/
 │   ├── BillingController.php
 │   ├── BillingService.php
 │   └── InvoiceRepository.php
 └── Users/
     ├── UserController.php
     ├── UserService.php
     └── UserRepository.php
```

Теперь всё, что касается заказов,  
находится в `Orders/`, и менять фичу можно локально.

---

### 3. Можно углублять структуру

В крупных системах фича — это не один класс, а целый **поддомен**.  
Её тоже можно делить по действиям или контекстам:

```
src/
 └── Orders/
     ├── CreateOrder/
     │   ├── Application/
     │   │   └── CreateOrderHandler.php
     │   ├── Domain/
     │   │   ├── OrderFactory.php
     │   │   └── OrderValidator.php
     │   └── Infrastructure/
     │       ├── OrderRepository.php
     │       └── OrderDatabaseSchema.sql
     ├── CancelOrder/
     └── GetOrderDetails/
```

Так можно совмещать **Package by Feature** и **Clean/Hexagonal Architecture**,  
где у каждой операции своя изоляция и слои (`Domain`, `Application`, `Infrastructure`) остаются внутри фичи, а не по всему проекту.

---

### 4. Преимущества

- 📦 **Локализованность:** всё для одной фичи рядом.
    
- 🧩 **Независимость:** легко вынести модуль в отдельный пакет или сервис.
    
- 🧠 **Понятная навигация:** разработчики мыслят “по задачам”, не по слоям.
    
- 🔄 **Упрощение рефакторинга:** фичи можно переписывать изолированно.
    

---

### 5. Когда особенно полезно

- Когда проект растёт и появляются десятки связанных модулей.
    
- Когда команда делится по направлениям (“оплаты”, “заказы”, “профили”).
    
- Когда готовишь проект к переходу на **модульную** или **микросервисную** структуру.


https://habr.com/ru/articles/911618/
