**DDD (Domain-Driven Design)** — это подход к проектированию сложных систем,  
в котором архитектура строится вокруг **предметной области** и **языка бизнеса**,  
а не вокруг таблиц, контроллеров или фреймворков.

---

### 1. Главная идея

DDD помогает не “делить код на папки”, а **делить систему на смыслы**.  
Главное — договориться, **как бизнес мыслит** и **что на самом деле происходит в домене**,  
а уже потом выразить это в коде.

> Если бизнес говорит “платёж подтверждён”,  
> то в коде должен быть метод `confirmPayment()`, а не `updateStatus(2)`.

---

### 2. Два уровня DDD

#### ⚙️ Тактический DDD

Определяет **как реализовать домен в коде**:

- Entity, Value Object, Aggregate, Repository, Domain Event
    
- Application / Domain / Infrastructure слои
    
- Событийная и командная модель поведения
    

Часто реализуется с помощью **Clean Architecture** или **Hexagonal Architecture**,  
которые обеспечивают ту самую изоляцию домена (ядро → порты → адаптеры).

#### 🧭 Стратегический DDD

Отвечает за **высокоуровневую структуру системы**:

- Определяет **Bounded Contexts** (границы смыслов)
    
- Строит **Context Map** (связи между контекстами)
    
- Делит систему на **Core / Supporting / Generic Subdomains**
    
- Задаёт, где мы строим собственную модель, а где интегрируемся
    

---

### 3. Bounded Context — граница смысла

**Bounded Context** — это изолированная область,  
где термины, модели и правила имеют **строго определённые значения**.

Например:

- В `Sales` “Order” — это заказ покупателя.
    
- В `Billing` “Order” — это счёт к оплате.
    
- В `Shipping` “Order” — это груз, готовый к отправке.
    

Одинаковое слово, разные модели — и это **нормально**,  
пока каждый контекст чётко отделён.

Каждый bounded context имеет:

- свой Ubiquitous Language,
    
- свой набор сущностей и value-объектов,
    
- свой интерфейс взаимодействия (API, events, adapters).
    

---

### 4. Ubiquitous Language — вездесущий язык

**Ubiquitous Language** — единый язык общения между бизнесом и разработкой.  
Он должен использоваться **в разговорах, документации и коде**.

Код должен звучать, как речь домена:

```php
$order->markAsPaid();
$invoice->generateFor($order);
```

а не как безликий CRUD:

```php
$repo->updateStatus($id, 2);
```

Этот язык живёт _внутри bounded context’а_ и эволюционирует вместе с ним.

---

### 5. Типичная структура DDD-модуля (на базе Clean Architecture)

```
src/Orders/
 ├── Application/
 │   ├── Command/
 │   └── Handler/
 ├── Domain/
 │   ├── Entity/
 │   ├── ValueObject/
 │   ├── RepositoryInterface.php
 │   └── DomainEvent.php
 └── Infrastructure/
     ├── DoctrineOrderRepository.php
     ├── OrderEventPublisher.php
     └── EntityMapping/
```

> Эта структура **не продиктована DDD**,  
> а заимствована из **Чистой Архитектуры (Clean Architecture)**  
> и **Гексагональной Архитектуры (Ports & Adapters)** —  
> потому что они идеально воплощают принципы изоляции домена,  
> заложенные в DDD.

---

### 6. Что даёт DDD

- 🧩 Разделение на логические контексты вместо случайных модулей.
    
- 💬 Код совпадает с терминологией бизнеса.
    
- 🚧 Изоляция домена от инфраструктуры (БД, HTTP, фреймворков).
    
- 🧱 Масштабирование — можно выделять bounded context в отдельный сервис.
    

---

### 7. Когда использовать

✅ Когда предметная область сложная и постоянно меняется.  
❌ Когда система — просто CRUD с минимумом логики.

DDD оправдан там, где **основная сложность — не в коде, а в смысле**.

---

### 8. Рекомендуемая литература

Книги очень Хононова часто рекомендуют, сам не читал


