
Подход, где **состояние инфраструктуры описано в Git**.

- Любое изменение проходит через PR, review и merge.
- CD-система отслеживает изменения в репозитории и автоматически применяет их (ArgoCD, FluxCD).
- Источник истины — Git, не “ручная админка”.
