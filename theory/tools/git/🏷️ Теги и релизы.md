
Теги — это версии кода.  
Лучше релизить именно с них.

Создать и запушить:

```bash
git tag -a v1.3.0 -m "Release 1.3.0"
git push origin v1.3.0
```

Посмотреть:

```bash
git tag --sort=-version:refname
```


