
Git — это не просто “система контроля версий”. Это твоя **машина времени** для кода, где можно:

- откатиться к любому состоянию,
    
- синхронизировать работу команды,
    
- вести параллельные ветки и релизы.
    

---

## 📦 Основы

| Команда                     | Что делает                                            |
| --------------------------- | ----------------------------------------------------- |
| `git clone <URL>`           | клонировать репозиторий                               |
| `git status`                | показать состояние (изменения, staged, untracked)     |
| `git add .`                 | добавить изменения в индекс                           |
| `git commit -m "msg"`       | зафиксировать изменения                               |
| `git push`                  | отправить коммиты на remote                           |
| `git pull`                  | обновить локальную ветку (эквивалент `fetch + merge`) |
| `git fetch`                 | только получить обновления, без merge                 |
| `git log --oneline --graph` | посмотреть историю                                    |
| `git diff`                  | показать различия                                     |
| `git stash`                 | временно спрятать изменения                           |
| `git cherry-pick <hash>`    | перенести коммит из другой ветки                      |
| `git rebase -i HEAD~5`      | интерактивный ребейз (редактирование истории)         |







