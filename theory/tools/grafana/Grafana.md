## Grafana

**Grafana** — это платформа визуализации метрик и наблюдаемости (observability).  
Она позволяет подключаться к источникам данных (Prometheus, Loki, ClickHouse, PostgreSQL и др.)  
и строить красивые **дашборды** с графиками, алертами и панелями.

![[Pasted image 20251104155837.png]]

---

### 1. Основная идея

Grafana — это **дашборд над временем**.  
Она не хранит данные сама (в отличие от Prometheus или Elastic),  
а выступает как “frontend для метрик”.

> Источники дают сырые данные → Grafana превращает их в графики и алерты.

---

### 2. Архитектура

- **Data sources** — откуда брать данные: Prometheus, InfluxDB, Loki, Tempo, Elasticsearch, PostgreSQL и т.п.
    
- **Dashboards** — панели с визуализацией (графики, таблицы, gauges, heatmaps).
    
- **Alerts** — правила уведомлений (email, Slack, Telegram, PagerDuty).
    
- **Users & Orgs** — управление доступом, папки, роли.
    

Grafana легко разворачивается в Docker или Kubernetes:

```bash
docker run -d -p 3000:3000 grafana/grafana
```

По умолчанию доступ:  
`http://localhost:3000` → login `admin` / password `admin`

---

### 3. Источники данных

Самые популярные источники:

- **Prometheus** — метрики сервисов (CPU, latency, FPM pool, Redis, PostgreSQL).
    
- **Loki** — логи, похож на Prometheus, но для текста.
    
- **Tempo / Jaeger** — распределённые трейсы.
    
- **Elasticsearch / OpenSearch** — сложный поиск и аналитика логов.
    
- **ClickHouse / PostgreSQL** — для кастомных отчётов и бизнес-метрик.
    

> Grafana умеет “джоинить” данные из разных источников на одном графике.

---

### 4. Типичные дашборды

#### Инфраструктура

- CPU / RAM / Disk usage
    
- HTTP latency (p95/p99)
    
- Количество активных PHP-FPM воркеров
    
- Ошибки 5xx, 4xx по nginx / ingress
    
- Redis hit/miss ratio
    
- PostgreSQL connections, replication lag
    

#### Приложение

- Количество запросов к API
    
- Очереди RabbitMQ: готовые, в работе, dead-letter
    
- Количество активных пользователей
    
- Ошибки PHP, исключения, timeout'ы
    
- Среднее время обработки задачи
    

#### Бизнес-метрики

- Заказы в час / день
    
- Конверсия
    
- Динамика дохода / подписок
    
- Платежные ошибки по провайдерам
    

---

### 5. Alerts (алерты)

Grafana умеет **алертинг на основе графиков**.  
Настраивается прямо в панели → “Alert rule”.

Пример:

> Если `php_fpm_queue_length > 10` в течение 5 минут — отправить в Telegram.

Интеграции:

- Email
    
- Slack / Telegram
    
- Webhook (можно триггерить собственную систему)
    
- PagerDuty / Opsgenie
    

---

### 6. Grafana + Prometheus = ❤️

На практике чаще всего Grafana используют вместе с Prometheus.

- Prometheus собирает метрики (`scrape targets`) с сервисов (PHP-FPM, Nginx, PostgreSQL, Redis).
    
- Grafana подключается к Prometheus и визуализирует их.
    
- Для алертов Prometheus или Grafana могут использовать общие правила.
    

**Пример метрики:**

```
php_fpm_active_processes{pool="www"} 3
```

**В Grafana (PromQL):**

```
rate(http_requests_total[5m])
```

---

### 7. Экосистема и плагины

Grafana имеет огромный каталог готовых:

- **Dashboards** — под Prometheus, Nginx, PHP-FPM, Redis, PostgreSQL и др.
    
- **Плагинов** — новые визуализации, интеграции и data sources.
    

📦 Каталог: [https://grafana.com/grafana/dashboards/](https://grafana.com/grafana/dashboards/)

---

### 8. Полезные плагины и тулзы

- **Loki** — хранение логов
    
- **Tempo** — трейсы (distributed tracing)
    
- **Prometheus** — метрики
    
- **Alertmanager** — маршрутизация уведомлений
    
- **Grafana Agent** — лёгкий агент для сбора метрик и логов
    
- **Grafana Alloy (ex. Agent)** — универсальный сборщик телеметрии
    

---

### 9. Как хранить и версионировать дашборды

Grafana хранит дашборды в SQLite или PostgreSQL,  
но их можно экспортировать в **JSON** и хранить в Git.  
Также есть Terraform-провайдеры для декларативного управления панелями.

---

### 10. Сравнение с другими

|Инструмент|Роль|
|---|---|
|**Grafana**|визуализация и алерты|
|**Prometheus**|сбор метрик|
|**Loki**|логи|
|**Tempo / Jaeger**|трейсы|
|**Kibana**|визуализация Elastic данных (ближайший аналог)|

---

### 💡 В одной фразе

> **Grafana** — это окно в твою инфраструктуру.  
> Она не хранит данные, но делает вид, что всё под контролем.


https://youtu.be/quxPM1kt_38?si=qjITMu8fziAZ2v-m
[[Prometheus]]
https://habr.com/ru/companies/flant/articles/462503/
