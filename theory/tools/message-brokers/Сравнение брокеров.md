# Сравнение брокеров сообщений

## RabbitMQ vs Redis vs PostgreSQL

|Критерий|RabbitMQ|Redis|PostgreSQL|
|---|---|---|---|
|**Протокол**|AMQP — надёжный и стандартизированный|нет|нет|
|**Durability**|disk-backed, ACK, retries|volatile (память)|есть, но дорого|
|**Гарантии доставки**|at-least-once, ack/nack|fire-and-forget|вручную|
|**Маршрутизация**|exchanges, routing keys, fanout|вручную через pub/sub|SQL + логика|
|**Нагрузка**|десятки тысяч сообщений/сек|быстро, но небезопасно|тяжело под нагрузкой|
|**Повторные попытки**|встроенные (`nack`, DLQ)|нужно писать вручную|руками и транзакциями|
|**Backpressure**|встроен (prefetch)|отсутствует|нет|
|**Уровень зрелости**|промышленный стандарт|вспомогательная роль|не предназначен|

Redis и Postgres подходят, если очередь — "второстепенная" фича.
RabbitMQ нужен, когда очередь — **основной механизм обмена данными**.

---

## RabbitMQ vs Kafka

### Архитектурные отличия

| Критерий | RabbitMQ | Kafka |
|---|---|---|
| **Тип системы** | Брокер сообщений (message broker) | Распределённый журнал событий (event log) |
| **Модель доставки** | Push (брокер отправляет) | Pull (consumer забирает) |
| **Хранение** | Удаляет после обработки | Хранит N дней/GB |
| **Порядок сообщений** | Гарантируется внутри очереди | Гарантируется внутри партиции |
| **Replay** | Нельзя перечитать | Можно перемотать offset |
| **Latency** | Низкая (миллисекунды) | Чуть выше (batch polling) |
| **Throughput** | Тысячи сообщений/сек | Миллионы сообщений/сек |
| **Маршрутизация** | Гибкая (exchanges, routing keys) | Простая (по topic и partition key) |
| **Backpressure** | Встроен (prefetch) | Consumer сам контролирует |
| **Complexity** | Проще в настройке | Сложнее (партиции, offset, rebalancing) |

---

### Когда использовать **RabbitMQ**

✅ **Задачи (tasks)** — фоновая обработка, отправка email, webhook'и.
✅ **RPC-паттерны** — запрос-ответ между сервисами.
✅ **Сложная маршрутизация** — нужны exchanges (fanout, topic, headers).
✅ **Низкая latency** — критична быстрая доставка сообщения.
✅ **Небольшие объёмы** — до десятков тысяч сообщений/сек.
✅ **Простота** — быстро настроить и начать работать.
✅ **Приоритеты сообщений** — критично обрабатывать важные задачи первыми.

---

### Когда использовать **Kafka**

✅ **Event Sourcing** — события как источник истины.
✅ **Stream Processing** — обработка потоков данных (аналитика, трансформации).
✅ **Высокая пропускная способность** — миллионы событий в секунду.
✅ **Log Aggregation** — централизованный сбор логов.
✅ **CDC (Change Data Capture)** — отслеживание изменений в БД.
✅ **Replay** — нужна возможность перечитать историю событий.
✅ **Долгое хранение** — события нужны не только сейчас, но и для аналитики/отладки.
✅ **Несколько consumer'ов** — разные системы читают одни и те же события.

---

### Гибридный подход

На практике часто используют **оба**:

```
Kafka — для событийного backbone (event bus)
  └─ RabbitMQ — для задач и команд (task queue)
```

Пример:
- **Kafka** собирает все события (orders.created, user.registered),
- **RabbitMQ** обрабатывает фоновые задачи (отправка email при order.created).

---

## 💡 В одной фразе

> **RabbitMQ** — это брокер, который умеет надёжно "донести" сообщение,
> даже если продюсер, воркер и сервер умерли по дороге.
> Redis и Postgres могут быть очередями, но RabbitMQ — им живёт.

> **Kafka** — это журнал событий для high-throughput сценариев.
> Если нужно обрабатывать миллионы событий и возвращаться к истории — это Kafka.
