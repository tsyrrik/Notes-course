**Стратегия кеширования** определяет, _в какой момент_ и _по каким правилам_  
данные попадают в кеш и обновляются в нём.  
Цель — ускорить доступ к часто запрашиваемым данным,  
но без устаревших или неверных результатов.

---

### 1. Основные типы стратегий

|Стратегия|Принцип работы|Когда применять|
|---|---|---|
|**Cache Aside (Lazy Loading)**|Приложение сначала ищет в кеше. Если нет — берёт из БД, кладёт в кеш, возвращает пользователю.|Самая распространённая, гибкая|
|**Read Through**|Кеш сам обращается к источнику данных при промахе.|Когда кеш интегрирован в библиотеку/ORM|
|**Write Through**|При записи данные сначала пишутся в кеш, а потом — в БД.|Когда важно, чтобы кеш и база всегда были синхронизированы|
|**Write Back (Write Behind)**|Запись идёт только в кеш, а обновление БД откладывается.|Для очень частых операций, где допустима небольшая рассинхронизация|
|**Refresh Ahead**|Кеш сам обновляет часто используемые данные до истечения TTL.|Для “горячих” данных, чтобы не было промахов|

---

### 2. Подробно о каждой стратегии

#### **Cache Aside (a.k.a. Lazy Loading)**

✅ Простая, гибкая и популярная.  
Приложение само управляет кешем.

```php
$data = $cache->get('user:42');
if (!$data) {
    $data = $db->query('SELECT * FROM users WHERE id=42');
    $cache->set('user:42', $data, ttl: 3600);
}
return $data;
```

Плюсы:

- Кеш заполняется только нужными данными.
    
- Не требует сложной инфраструктуры.
    

Минусы:

- Первый запрос после истечения TTL будет медленным (“cold start”).
    
- Возможны устаревшие данные при изменениях в БД.
    

---

#### **Read Through**

Кеш сам знает, как загрузить данные при промахе.  
Приложение просто делает `$cache->get(key)`.

> Пример: Redis с RedisGears, Doctrine Cache, Spring Cache.

Плюсы:

- Код приложения чище — логика кеша внутри драйвера.
    
- Кеш гарантированно согласован.
    

Минусы:

- Сложнее тестировать и отлаживать.
    
- Меньше контроля на уровне приложения.
    

---

#### **Write Through**

Каждая запись идёт и в кеш, и в БД.  
Если запись прошла успешно — данные в кеше всегда актуальны.

```php
$cache->set('user:42', $data);
$db->update('users', $data);
```

Плюсы:

- Нет устаревших данных в кеше.
    
- Подходит для систем, где читают чаще, чем пишут.
    

Минусы:

- Каждая запись — двойная операция.
    
- Медленнее, если БД часто обновляется.
    

---

#### **Write Back (Write Behind)**

Запись идёт **сначала в кеш**, потом (асинхронно) в БД.  
Быстро, но с риском потери при сбое.

> Часто реализуется через очередь (Kafka, RabbitMQ).

Плюсы:

- Высокая производительность при массовых записях.
    
- БД разгружается.
    

Минусы:

- Возможна рассинхронизация.
    
- Требует фонового “флашера” для записи изменений.
    

---

#### **Refresh Ahead**

Кеш отслеживает “популярные” ключи и **обновляет их заранее**,  
чтобы пользователи никогда не видели “холодные” запросы.

Плюсы:

- Нет задержек из-за промахов.
    
- Подходит для горячих данных (категории, статистика).
    

Минусы:

- Увеличивает нагрузку на источник данных.
    
- Сложно реализовать оптимально (надо предсказывать “горячие” ключи).
    

---

### 3. Часто комбинируют

На практике обычно сочетают несколько подходов:

- **Cache Aside** для большинства данных.
    
- **Write Through** для критичных конфигураций.
    
- **Refresh Ahead** для горячих страниц.
    

Пример из реальной жизни (Redis + PHP):

- читаем через Cache Aside,
    
- обновляем при записи через `invalidate()`,
    
- фоновый крон обновляет популярные ключи.
    

---

### 4. TTL и инвалидация

TTL (Time To Live) ограничивает срок жизни кэшируемых данных.  
Но в продакшне часто используют **умную инвалидацию**:

- при изменении сущности в БД — сразу сбрасываем соответствующий ключ (`user:42`);
    
- или используем “tag-based” инвалидацию (`cache:users:*`).
    

> Главное правило: **“Invalidate smarter than you cache.”**

---

### Связанные заметки

- [[Стратегии вытеснения кеша (Cache Eviction Policies)]] — как выбирать, какие ключи удалять, когда память ограничена.
- [[Прогрев кеша]] — как подготовить кеш после деплоя, чтобы избежать холодных запросов.
- [[CDN (Content Delivery Network)]] — пример распределённого кеша на «передовой».
- [[Продуманная оптимизация]]

### 💡 В одной фразе

> **Стратегия кеширования** определяет, _когда и как данные попадают в кеш_ —
> лениво, заранее, синхронно или отложенно.
> Нет универсального варианта, но комбинация Cache Aside + TTL закрывает 90% случаев.

