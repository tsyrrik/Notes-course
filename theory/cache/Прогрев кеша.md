**Прогрев кеша** — это процесс **предварительного заполнения кеша** актуальными данными  
до того, как придут реальные запросы пользователей.  
Цель — чтобы система работала быстро с первых секунд после деплоя, рестарта или очистки кеша.

---

### 1. Зачем нужен прогрев кеша

После очистки кеша или перезапуска приложения:

- первый пользователь “попадает” в холодный кеш;
    
- запросы обрабатываются медленнее;
    
- может быть повышенная нагрузка на БД или API.
    

Прогрев решает это заранее:

- готовит кеш с нужными ключами;
    
- снижает пики нагрузки после релизов;
    
- ускоряет первый ответ после деплоя или сброса кешей.
    

---

### 2. Типичные ситуации, где нужен прогрев

✅ **HTTP-кеш / [[CDN (Content Delivery Network)|CDN]]** — заранее пройтись по популярным страницам.
✅ **Doctrine / ORM кеш** — скомпилировать метаданные и запросы.  
✅ **Template кеш (Twig, Blade)** — прогрузить шаблоны после деплоя.  
✅ **Application cache (Redis, Memcached)** — заполнить кэшируемые данные (например, конфигурации, списки категорий).  
✅ **Search index cache** — прогреть результаты популярных поисков.  
✅ **API Gateway** — обновить JWT public keys, конфигурацию маршрутов и т.д.

---

### 3. Как выглядит процесс

#### Пример (Symfony)

```bash
php bin/console cache:clear
php bin/console cache:warmup
```

Symfony сам вызывает “warmers”, зарегистрированные в ядре:

- скомпилированные контейнеры DI;
    
- маршруты;
    
- шаблоны;
    
- метаданные ORM.
    

Можно добавить свои:

```php
class MyCacheWarmer implements CacheWarmerInterface
{
    public function warmUp(string $cacheDir)
    {
        // например, заранее дернуть популярные страницы
    }

    public function isOptional(): bool => true;
}
```

---

### 4. Прогрев для Redis / API

Можно написать CLI-команду, которая:

- получает список “популярных” ключей или URL;
    
- делает GET-запросы к ним;
    
- тем самым заставляет систему сохранить результаты в кеш.
    

#### Пример:

```bash
curl -s https://myapp.com/api/products?page=1 > /dev/null
curl -s https://myapp.com/api/categories > /dev/null
```

или на PHP:

```php
$popularEndpoints = ['/api/home', '/api/products?page=1', '/api/blog'];
foreach ($popularEndpoints as $url) {
    file_get_contents("https://myapp.com$url");
}
```

---

### 5. Виды прогрева

|Тип|Когда используется|Пример|
|---|---|---|
|**Cold warming**|после деплоя, очистки кеша|`bin/console cache:warmup`|
|**Hot warming**|во время работы (фоново)|Cron или worker обновляет ключи|
|**Predictive warming**|на основе статистики обращений|“умный” прогрев популярных страниц|

---

### 6. Где запускать

- В [[CI CD|CI/CD]] pipeline после деплоя.

- В [[Cron]] после ежедневной очистки кешей.

- В фоне (например, через [[RabbitMQ]] worker).

- При старте контейнера или сервиса.
    

---

### 7. Советы

- Не прогревай **всё подряд** — только реально востребованные данные.

- Для тяжёлых кешей (CDN, Redis) используй **батчинг / rate limit**, чтобы не уронить систему.

- Автоматизируй — кеш должен греться без участия человека.

- Для CDN (Cloudflare, Fastly, Varnish) есть API для “prefetch”.


---

### 🔗 Связанные заметки

- [[Стратегии кеширования (Cache Strategies)]] и [[Стратегии вытеснения кеша (Cache Eviction Policies)]] — помогают выбрать, какие данные стоит греть и как долго их хранить.
- [[CDN (Content Delivery Network)]] — про edge-кеширование и работу с глобальными сетями доставки контента.
- [[CI CD]] — где можно автоматизировать прогрев после деплоя.

---

### 💡 В одной фразе

> **Прогрев кеша** — это подготовка системы к нагрузке,
> чтобы пользователи не ждали, пока всё “само закешируется”.
