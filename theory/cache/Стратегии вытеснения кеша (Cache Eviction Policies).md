Когда кэш достигает лимита (памяти, количества ключей и т.п.),  
ему нужно **удалить** часть данных, чтобы освободить место.  
Механизм выбора, _что именно удалить_, называется **стратегией вытеснения**.

---

### 1. Зачем это нужно

- Кэш не безграничен — память ограничена.
    
- Старые, редко используемые данные занимают место.
    
- Нужно балансировать между **актуальностью** и **частотой обращений**.
    

Хорошая политика вытеснения:

- сохраняет “горячие” данные в памяти,
    
- удаляет “холодные”,
    
- не допускает деградации производительности.
    

---

### 2. Основные стратегии

|Стратегия|Расшифровка|Принцип|Когда применять|
|---|---|---|---|
|**LRU**|Least Recently Used|Удаляет _наименее недавно использованные_ ключи|Универсальный вариант (Redis, Memcached)|
|**LFU**|Least Frequently Used|Удаляет _наименее часто используемые_ ключи|Для стабильных паттернов обращений|
|**FIFO**|First In, First Out|Удаляет _самые старые_ элементы (по времени добавления)|Просто, но может выбросить часто используемые данные|
|**MRU**|Most Recently Used|Удаляет _самые недавно использованные_|Для данных, которые редко запрашиваются дважды подряд|
|**Random**|—|Случайное удаление|Применяется, когда важна скорость, а не точность|
|**TTL-based**|Time To Live|Элемент удаляется после заданного времени жизни|Для данных с естественным сроком актуальности|

---

### 3. Примеры поведения (на 3 слота кеша)

#### LRU (Least Recently Used)

```
put A,B,C        → [A,B,C]
get A            → [B,C,A]
put D (нет места) → [C,A,D]  ← A недавно использован, остаётся
```

#### FIFO (First In, First Out)

```
put A,B,C        → [A,B,C]
put D            → [B,C,D]  ← вылетел A (первый вошёл)
```

#### LFU (Least Frequently Used)

```
get A (3 раза), get B (1 раз)
put D → удалится B, т.к. у него наименьшая частота
```

---

### 4. TTL и Expiration

TTL (Time To Live) — дополнительный механизм:

- элемент хранится не дольше заданного времени (например, 300 секунд);
    
- даже если не вытеснен, он истечёт сам;
    
- многие системы (Redis, CDN, HTTP Cache) совмещают **LRU + TTL**.
    

---

### 5. Как это реализовано в Redis

Redis поддерживает несколько политик (через `maxmemory-policy`):

|Политика|Описание|
|---|---|
|`noeviction`|ошибка при переполнении|
|`allkeys-lru`|LRU по всем ключам|
|`volatile-lru`|LRU только по ключам с TTL|
|`allkeys-random`|случайное удаление любого ключа|
|`volatile-ttl`|удаляет ближайшие к истечению TTL|
|`allkeys-lfu`|LFU по всем ключам (современный вариант)|

Пример:

```conf
maxmemory 256mb
maxmemory-policy allkeys-lfu
```

---

### 6. Советы по выбору стратегии

|Сценарий|Рекомендация|
|---|---|
|Часто запрашиваемые ключи|**LRU** или **LFU**|
|Предсказуемое устаревание данных|**TTL** или **volatile-ttl**|
|Ограниченная память, простая логика|**FIFO**|
|Статичные данные без повторных запросов|**MRU** (редко, но встречается)|

---

### Связанные заметки

- [[Стратегии кеширования (Cache Strategies)]] — когда и как данные попадают в кеш, чтобы понимать, что потом нужно вытеснять.
- [[Прогрев кеша]] — о том, как заполнить кеш нужными ключами после очистки или деплоя.
- [[CDN (Content Delivery Network)]] — пример системы, где выбор политики вытеснения влияет на отдачу контента.

### 💡 В одной фразе

> **Стратегия вытеснения** — это способ сказать кешу,
> “что выкинуть, когда место закончилось”,
> чтобы остались самые полезные данные.
