**VACUUM** — это процесс очистки “мёртвых” строк в PostgreSQL,  
которые остались после `UPDATE` и `DELETE`.

> PostgreSQL не перезаписывает строки при изменении — он создаёт новые версии,  
> а старые помечает как “устаревшие” (dead tuples).  
> VACUUM их потом удаляет, чтобы освободить место и обновить статистику.

---

### 1. Почему без VACUUM всё ломается

PostgreSQL — **MVCC** (Multi-Version Concurrency Control) система.  
Каждая транзакция видит “свой срез” данных,  
поэтому при обновлениях старая версия строки сохраняется,  
чтобы другие транзакции могли её дочитать.

Со временем таблица превращается в свалку старых версий:

|Операция|Что реально происходит|
|---|---|
|`UPDATE users SET name='Bob'`|создаётся новая строка с новым `xmin`, старая помечается “dead”|
|`DELETE FROM users WHERE id=1`|строка не удаляется — только помечается как “dead”|
|`SELECT * FROM users`|всё ещё видит старую версию, если транзакция началась раньше|

> Эти “трупы” занимают место на диске и замедляют последовательное чтение.

VACUUM приходит и вычищает их.

---

### 2. Типы VACUUM

|Команда|Что делает|Когда использовать|
|---|---|---|
|`VACUUM`|удаляет dead tuples, но не уменьшает размер файла|обычно выполняется autovacuum|
|`VACUUM FULL`|переписывает всю таблицу, реально освобождая диск|редко, вручную, при сильном раздувании|
|`ANALYZE`|обновляет статистику для оптимизатора|выполняется часто автоматически|
|`VACUUM ANALYZE`|комбинация двух|после массовых операций (`COPY`, `DELETE`, `UPDATE`)|

---

### 3. Автоматический VACUUM

PostgreSQL сам запускает **autovacuum** при необходимости.  
Он отслеживает количество изменённых строк в таблице и решает,  
когда пора чистить.

Настройки (`postgresql.conf`):

```ini
autovacuum = on
autovacuum_vacuum_scale_factor = 0.2     # 20% изменённых строк
autovacuum_analyze_scale_factor = 0.1    # 10% для анализа
autovacuum_naptime = 60s                 # интервал между проверками
```

> В продакшене часто занижают scale_factor,  
> чтобы VACUUM срабатывал чаще и не приходилось чистить горы мусора разом.

---

### 4. Что делает VACUUM под капотом

1. Сканирует таблицу.
    
2. Находит “мёртвые” строки, которые уже никем не видны.
    
3. Помечает эти страницы как “free space”.
    
4. Обновляет `visibility map` и статистику.
    
5. При необходимости записывает в `pg_freespace` информацию о свободных страницах.
    

> При этом VACUUM **не блокирует таблицу** (в отличие от FULL-версии).  
> Он может выполняться параллельно с обычными запросами.

---

### 5. VACUUM FULL

```sql
VACUUM FULL users;
```

- Блокирует таблицу на запись.
    
- Переписывает всю таблицу в новый файл.
    
- Возвращает физическое место операционной системе.
    
- Полезен, если таблица раздулась в разы после массового удаления.
    

> После `DELETE FROM logs WHERE date < NOW() - interval '6 months'`  
> — можно сделать `VACUUM FULL logs;`.

---

### 6. bloat (раздувание таблиц)

Понять, насколько таблица “распухла”, можно так:

```sql
SELECT
  schemaname, relname,
  pg_size_pretty(pg_table_size(oid)) AS table_size,
  pg_size_pretty(pg_total_relation_size(oid)) AS total_size,
  (pg_total_relation_size(oid) - pg_table_size(oid)) / pg_table_size(oid)::float AS bloat_ratio
FROM pg_class
WHERE relkind = 'r'
ORDER BY bloat_ratio DESC
LIMIT 10;
```

> Если `bloat_ratio > 0.3` — пора вручную VACUUM FULL или CLUSTER.

---

### 7. Freeze и xid wraparound

Каждая строка имеет системные поля `xmin` и `xmax` — номера транзакций.  
PostgreSQL использует **32-битные transaction IDs**,  
поэтому они могут “переполниться” примерно через 2 миллиарда транзакций.

VACUUM также выполняет **freezing** —  
помечает старые строки как “замороженные”, чтобы предотвратить wraparound.

> Если не вакуумить вообще, Postgres **отключит запись**,  
> чтобы не потерять консистентность (panicking stop).

---

### 8. Диагностика и статистика

Проверить статистику по autovacuum:

```sql
SELECT relname, n_tup_ins, n_tup_upd, n_tup_del,
       n_dead_tup, last_autovacuum, vacuum_count
FROM pg_stat_all_tables
ORDER BY n_dead_tup DESC
LIMIT 10;
```

> `n_dead_tup` показывает количество “мёртвых” строк,  
> `last_autovacuum` — когда таблица последний раз чистилась.

---

### 9. VACUUM и производительность

- Частый `VACUUM` → таблицы компактны, индексы точнее, меньше I/O.
    
- Редкий `VACUUM` → растёт bloat, запросы медленнее, диск пухнет.
    
- `autovacuum_freeze_max_age` ≈ 200 млн — не дай ему переполниться.
    

💡 Совет: для больших таблиц ставь отдельные параметры:

```sql
ALTER TABLE logs SET (autovacuum_vacuum_scale_factor = 0.05);
```

---

### 10. Аналогии с другими системами

|СУБД / система|Аналог|
|---|---|
|**MySQL (InnoDB)**|background purge|
|**MongoDB**|compaction|
|**Elasticsearch**|segment merge|
|**Kafka**|log compaction|
|**ClickHouse**|merge parts|

> Везде идея одна: **новые данные создаются рядом, старые сжимаются или выбрасываются.**

---

### 💡 В одной фразе

> **VACUUM** — это мусорщик PostgreSQL.  
> Он подметает за транзакциями, которые ушли,  
> чтобы база не превратилась в кладбище версий и не взорвалась от собственного веса.

