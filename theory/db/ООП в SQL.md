
SQL — язык реляционный, не объектно-ориентированный.  
Но современные СУБД (PostgreSQL, Oracle, SQL Server) позволяют использовать **объектные идеи**:  
типы, наследование, функции, триггеры, представления и т. п.

---

### 1. Общая идея

ООП в контексте SQL — это не про классы и методы в привычном смысле,  
а про то, **как сделать структуры данных более “объектными”**:

- описывать сущности с атрибутами,
    
- использовать наследование таблиц или типов,
    
- хранить сложные структуры как поля,
    
- создавать поведение (функции, триггеры), связанное с этими структурами.
    

---

### 2. Наследование таблиц (PostgreSQL)

Postgres поддерживает **наследование таблиц**, почти как классов.

```sql
CREATE TABLE vehicle (
  id serial PRIMARY KEY,
  name text,
  created_at timestamp default now()
);

CREATE TABLE car (
  doors int
) INHERITS (vehicle);

INSERT INTO car (name, doors) VALUES ('Audi A4', 4);

SELECT * FROM vehicle;
-- вернёт и строки из car, и из vehicle
```

#### Особенности:

- “Родительская” таблица видит строки потомков.
    
- Можно запрашивать отдельно (`ONLY vehicle`).
    
- Наследуются колонки, индексы, ограничения.
    
- Под капотом — это просто связь “view + union”.
    

---

### 3. Пользовательские типы (Composite types)

Можно создавать **собственные типы данных**, напоминающие объекты.

```sql
CREATE TYPE address AS (
  city text,
  street text,
  zip text
);

CREATE TABLE users (
  id serial PRIMARY KEY,
  name text,
  home address
);

INSERT INTO users (name, home)
VALUES ('Ivan', ROW('Tomsk', 'Lenina 10', '634000')::address);

SELECT (home).city FROM users;
```

> Это похоже на объект с полями.  
> Composite type можно передавать в функции, возвращать и хранить в JSON.

---

### 4. Методы и поведение (функции + триггеры)

Вместо методов классов — **функции и триггеры**, которые реализуют поведение:

```sql
CREATE FUNCTION vehicle_age(created timestamp)
RETURNS int AS $$
SELECT extract(year from age(now(), created));
$$ LANGUAGE sql;

SELECT name, vehicle_age(created_at) FROM vehicle;
```

А “события” (например, обновление времени) делаются через **триггеры**.  
По сути, это аналог “методов”, срабатывающих при изменении состояния объекта.

---

### 5. Полиморфизм (через JSON и функции)

В PostgreSQL можно хранить разнородные “объекты” в одном поле — через `jsonb`.

```sql
CREATE TABLE events (
  id serial,
  type text,
  payload jsonb
);
```

И вызывать поведение в зависимости от типа:

```sql
CREATE FUNCTION process_event(e events) RETURNS void AS $$
BEGIN
  CASE e.type
    WHEN 'user_created' THEN PERFORM handle_user_created(e.payload);
    WHEN 'order_paid' THEN PERFORM handle_order_paid(e.payload);
  END CASE;
END;
$$ LANGUAGE plpgsql;
```

> Это примитивная форма **полиморфизма**:  
> поведение зависит от типа данных, а не от структуры таблицы.

---

### 6. Объектно-реляционная модель (ORDBMS)

Некоторые СУБД (например, PostgreSQL и Oracle) считаются **объектно-реляционными**.  
Они позволяют:

- создавать пользовательские типы и наследование,
    
- использовать методы над типами,
    
- хранить массивы и вложенные структуры,
    
- комбинировать SQL и процедурный язык (PL/pgSQL, PL/Python).
    

---

### 7. Где это уместно

✅ Можно:

- моделировать иерархии сущностей (транспорт, пользователи, заказы);
    
- хранить сложные структуры (адрес, геолокация, параметры);
    
- создавать функции, инкапсулирующие логику на уровне БД.
    

🚫 Не стоит:

- превращать SQL в “ORM на SQL”;
    
- хранить поведение, которое логичнее вынести в код приложения;
    
- использовать наследование, если проще сделать `type` или `enum`.
    
