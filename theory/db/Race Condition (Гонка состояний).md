**Race condition** — это ситуация, когда **результат программы зависит от порядка выполнения** нескольких конкурентных операций.

> Две или более части кода “соревнуются” за доступ к одному ресурсу,  
> и кто успеет первым — тот и определяет итог.

---

### 1. Простое объяснение

Представь:

1. Два пользователя одновременно читают баланс `100`.
    
2. Оба решают “прибавить +10”.
    
3. Каждый делает `balance = 100 + 10`.
    
4. В итоге баланс становится `110`, а не `120`.
    

> Это классическая **гонка на чтение-запись**: оба читают старое значение и перезаписывают друг друга.

---

### 2. Пример на PHP

```php
// Оба потока выполняют это почти одновременно
$balance = $db->query('SELECT balance FROM accounts WHERE id=1')->fetchColumn();
$newBalance = $balance + 10;
$db->query('UPDATE accounts SET balance = ? WHERE id=1', [$newBalance]);
```

Если запросы выполняются параллельно,  
оба читают `100`, оба пишут `110`.  
Результат — потеря одного обновления (_lost update_).

---

### 3. Где обычно возникают гонки

|Сценарий|Пример|
|---|---|
|💾 **База данных**|Два запроса изменяют одну запись без транзакций|
|🧵 **Многопоточность**|Несколько потоков пишут в общий объект|
|🌐 **Web API**|Два запроса одновременно модифицируют одну сущность|
|🕓 **Асинхронные задачи**|Несколько воркеров берут одну и ту же задачу|
|📁 **Файлы / кеш**|Два процесса записывают в один и тот же файл|

---

### 4. Виды race condition

|Тип|Что происходит|
|---|---|
|**Read-Modify-Write**|обе операции читают одно и то же значение и пишут новое (пример с балансом)|
|**Check-Then-Act**|проверка и действие выполняются неатомарно|
|**Initialization race**|несколько потоков инициализируют один и тот же ресурс|
|**Time-of-check-to-time-of-use (TOCTTOU)**|данные меняются между проверкой и использованием|

Пример TOCTTOU:

```php
if (file_exists($path)) {
    file_put_contents($path, $data); // но другой процесс мог удалить файл между этими действиями
}
```

---

### 5. Как решать (в БД)

1. **Транзакции**  
    Все операции над одним ресурсом должны быть атомарны:
    
    ```sql
    BEGIN;
    SELECT balance FROM accounts WHERE id=1 FOR UPDATE;
    UPDATE accounts SET balance = balance + 10 WHERE id=1;
    COMMIT;
    ```
    
    `FOR UPDATE` ставит блокировку на строку, не давая другому потоку прочитать старое значение.
    
2. **Optimistic Locking (оптимистическая блокировка)**  
    Добавляем поле `version` и проверяем, не изменилось ли оно:
    
    ```sql
    UPDATE accounts
    SET balance = balance + 10, version = version + 1
    WHERE id = 1 AND version = 3;
    ```
    
    Если `version` не совпала — кто-то уже обновил запись → повторяем попытку.
    
3. **Atomic operations / UPSERT**  
    Используем атомарные SQL-операции:
    
    ```sql
    UPDATE accounts SET balance = balance + 10 WHERE id = 1;
    ```
    
    (без промежуточного SELECT — чтение и запись в одной инструкции)
    

---

### 6. Как решать в коде

|Подход|Суть|
|---|---|
|**Mutex / Lock / Semaphore**|только один поток может менять значение в данный момент|
|**Atomic operations**|например, `atomic_add()` вместо `x = x + 1`|
|**Queues / Job systems**|делегируем операции в очередь, выполняем последовательно|
|**Idempotency**|делаем операции безопасными при повторном выполнении|
|**Versioning / Compare-and-swap (CAS)**|обновляем значение, только если оно не изменилось|

---

### 7. Пример с Redis

Redis помогает бороться с гонками через **atomic commands**:

```bash
INCR user:1:balance
```

или через **distributed lock**:

```bash
SETNX lock:balance 1
EXPIRE lock:balance 10
```

> Redis-команды выполняются последовательно в одном потоке,  
> поэтому внутри самого Redis гонок нет, если не лезть из двух клиентов одновременно.

---

### 8. Почему это трудно ловить

- Ошибка не воспроизводится стабильно.
    
- На локальной машине всё работает.
    
- Проявляется только при высокой нагрузке.
    
- Иногда приводит не к падению, а просто к “неверным данным”.
    

> Гонка — это не ошибка в логике, а ошибка во **времени выполнения**.

---

### 9. Инструменты диагностики

- В PHP — логирование timestamp и thread/request id.
    
- В PostgreSQL — `pg_locks`, `pg_stat_activity` (покажет конкурирующие транзакции).
    
- В многопоточных программах — `thread sanitizer`, `race detector` (Go, C++, Rust).
    

---

### 10. Итог

|Сигнал тревоги|Что делать|
|---|---|
|Несогласованные данные|добавить транзакции / блокировки|
|“Пропавшие” обновления|использовать атомарные SQL-операции|
|Случайные ошибки при параллельных запросах|проверить очередь или синхронизацию|

---

### 💡 В одной фразе

> **Race condition** — это когда программа ведёт себя по-разному,  
> в зависимости от того, кто оказался быстрее.  
> Решение — не надеяться на удачу, а сделать действия атомарными и предсказуемыми.
