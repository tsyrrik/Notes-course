**Пагинация (pagination)** — это способ получать результаты запроса **постепенно**,  
чтобы не тянуть всё сразу в память и не перегружать клиент или сеть.

---

### 1. OFFSET / LIMIT (классическая пагинация)

Самый привычный способ:

```sql
SELECT * FROM users ORDER BY id LIMIT 50 OFFSET 100;
```

#### ⚙️ Как работает

База читает первые 150 строк, потом отбрасывает 100 и возвращает оставшиеся 50.  
Параллельно почти всегда нужно знать **общее количество записей**,  
чтобы вывести “Страница 3 из N”.

```sql
SELECT COUNT(*) FROM users;  -- для расчёта total
```

#### 🧨 Проблема

`COUNT(*)` на большой таблице (миллионы строк, без узких фильтров) — **тяжёлая операция**:  
она требует полного сканирования, если нет подходящего индекса.

Например, если вы показываете список email-пользователей или логов,  
подсчёт total каждую секунду может стать бутылочным горлышком.

#### 👍 Плюсы

- Простая реализация.
    
- Удобна для UI с “страницами”.
    

#### 👎 Минусы

- Медленно при большом OFFSET.
    
- Требует `COUNT(*)`, что дорого на больших данных.
    
- Неконсистентна при изменениях данных — смещения “плавают”.
    

✅ Подходит для:

- Админок и интерфейсов с небольшими выборками.
    

---

### 2. Keyset Pagination (Seek Pagination)

```sql
SELECT * FROM users
WHERE id > 500
ORDER BY id
LIMIT 50;
```

#### ⚙️ Как работает

Запоминается **последний ключ** (например, `id`) с предыдущей страницы.  
Следующий запрос выбирает записи “после” него.

#### 👍 Плюсы

- Использует индекс — очень быстро.
    
- Не требует `COUNT(*)`.
    
- Устойчив к изменению данных.
    

#### 👎 Минусы

- Нельзя просто прыгнуть на страницу №10.
    
- Нужен уникальный сортировочный ключ (id, created_at).
    

✅ Подходит для:

- Просмотра больших наборов данных (логов, email-таблиц).
    
- API и бесконечных списков.
    

---

### 3. Cursor-based Pagination (API / GraphQL стиль)

То же самое, но с **токеном курсора**, который кодирует позицию:

```graphql
query {
  users(first: 50, after: "Y3Vyc29yOjUwMA==") {
    edges { node { id email } }
    pageInfo { hasNextPage endCursor }
  }
}
```

#### 👍 Плюсы

- Безопасна для API.
    
- Отлично подходит для real-time и infinite scroll.
    
- Не требует total.
    

#### 👎 Минусы

- Сложнее реализовать (encode/decode курсора).
    
- Нет понятия “страницы №3”.
    


---

### 5. Когда и что выбирать

| Цель                               | Подход                  | Комментарий                |
| ---------------------------------- | ----------------------- | -------------------------- |
| UI со страницами (до 100k записей) | OFFSET/LIMIT + COUNT(*) | Просто, но может тормозить |
| Большие данные, real-time          | Keyset или Cursor       | Быстро, не требует total   |
| Публичное API                      | Cursor                  | Безопасно и масштабируемо  |

---

### 💡 Важное замечание

> Классическая пагинация с “Страница 1 из 1000” требует `COUNT(*)`.  
> На миллионах строк это дорогая операция, особенно если фильтрация сложная.
> 
> В таких случаях **перейди на курсорную или keyset-пагинацию** —  
> она не считает общее количество и масштабируется куда лучше.
