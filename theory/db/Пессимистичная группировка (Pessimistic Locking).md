**Пессимистичная группировка** — это стратегия конкурентного доступа,  
при которой данные **блокируются сразу при чтении**,  
чтобы никто другой не мог их изменить, пока транзакция не завершится.

> Идея: “я не доверяю никому — лучше заблокирую сейчас, чем чинить потом”.

---

### 1. Контекст

Пессимистичный подход противоположен оптимистичному:

|Подход|Принцип|Пример|
|---|---|---|
|**Оптимистичный**|не блокируем, проверяем версию при коммите|REST, API, UI|
|**Пессимистичный**|блокируем сразу, не даём другим писать|транзакции, финансы|

> Применяется там, где **нельзя допустить конфликт вообще**,  
> даже если это замедлит систему.

---

### 2. Как это работает

1. Транзакция выбирает данные **с блокировкой**.
    
2. Другие транзакции, пытающиеся изменить эти данные, **ждут**.
    
3. Когда первая транзакция завершится (`COMMIT` / `ROLLBACK`) — блокировка снимается.
    
4. Только после этого другие могут продолжить.
    

---

### 3. Пример в PostgreSQL

```sql
BEGIN;

SELECT * FROM accounts
WHERE id = 1
FOR UPDATE;

UPDATE accounts
SET balance = balance - 100
WHERE id = 1;

COMMIT;
```

- `FOR UPDATE` ставит **эксклюзивную блокировку** на строку.
    
- Любая другая транзакция, пытающаяся обновить `id = 1`, будет ждать.
    

---

### 4. Типы блокировок в PostgreSQL

|Ключевое слово|Что делает|
|---|---|
|`FOR UPDATE`|блокирует строку от любых изменений|
|`FOR NO KEY UPDATE`|мягче — блокирует без изменения ключа|
|`FOR SHARE`|разрешает читать, но не менять|
|`FOR KEY SHARE`|только защищает внешний ключ|

> Можно настраивать уровень “агрессивности” блокировки,  
> в зависимости от того, что именно нужно защитить.

---

### 5. Пример конфликта

```sql
-- Транзакция A
BEGIN;
SELECT * FROM users WHERE id=1 FOR UPDATE;

-- Транзакция B
BEGIN;
UPDATE users SET name='Alice' WHERE id=1; -- ждёт A

-- Транзакция A
COMMIT; -- только теперь B продолжит
```

> Пока первая транзакция не завершится, вторая “висит”.  
> Это гарантирует, что запись не изменится “между проверкой и действием”.

---

### 6. Когда применять

✅ Хорошо подходит:

- Для **финансовых операций**, где ошибка неприемлема;
    
- При **последовательных шагах** (резервы, заказы, билеты);
    
- Когда **транзакции короткие**, но критичные;
    
- Когда данные “горячие” и часто обновляются.
    

❌ Плохо:

- В API с длительными транзакциями;
    
- В системах с высокой конкуренцией (много параллельных апдейтов);
    
- При долгих запросах (может привести к **deadlock’ам**).
    

---

### 7. Отличие от Optimistic Locking

|Критерий|Пессимистичная|Оптимистичная|
|---|---|---|
|Механизм|блокировка строк|проверка версии|
|Поведение|ждёт|перезапускает|
|Производительность|ниже|выше|
|Безопасность|гарантированная|условная|
|Риск deadlock|есть|нет|

> Пессимистичный — “безопасно, но медленно”.  
> Оптимистичный — “быстро, но с шансом конфликта”.

---

### 8. В ORM и фреймворках

#### Doctrine (PHP)

```php
$user = $em->find(User::class, 1, LockMode::PESSIMISTIC_WRITE);
```

#### SQLAlchemy (Python)

```python
session.query(User).with_for_update().first()
```

> Оба делают то же самое: `SELECT ... FOR UPDATE`.

---

### 9. Потенциальные проблемы

- **Deadlock** — если две транзакции блокируют ресурсы в разном порядке.
    
- **Lock contention** — массовые ожидания при высокой нагрузке.
    
- **Долгие транзакции** — блокируют остальных и могут “подвесить” систему.
    

💡 Лучшее решение — **держать транзакции короткими** и **брать блокировку как можно позже** (перед самым обновлением).

---

### 10. Комбинированный подход

Иногда используют **гибрид**:

- Оптимистичный — по умолчанию,
    
- Пессимистичный — в узких местах (например, переводы, билеты).
    

> Можно начать с optimistic и перейти к pessimistic,  
> когда статистика покажет, что конфликты происходят слишком часто.

---

### 💡 В одной фразе

> **Пессимистичная группировка** — это “замок на двери данных”.  
> Ты никого не впускаешь, пока не закончишь,  
> потому что доверие — хорошо, а блокировки надёжнее.