
**Шардирование** — это способ **разделить данные по нескольким независимым узлам (нодам)**,  
чтобы обрабатывать больше объёма и нагрузки, чем способен один сервер.

> Если партиционирование делит таблицу **внутри одной БД**,  
> то **шардирование** делит данные **между несколькими базами или кластерами**.

---

### 1. Зачем нужно шардирование

|Проблема|Что решает|
|---|---|
|Таблица выросла до сотен гигабайт|Делим данные на независимые куски|
|Одна база не справляется по CPU / IO|Распределяем нагрузку между нодами|
|Высокая латентность при глобальных пользователях|Разносим шардированные БД ближе к регионам|
|Хотим масштабирование без вертикального апгрейда|Добавляем ноды горизонтально|

> Шардирование — путь к **горизонтальному масштабированию** (horizontal scaling).

---

### 2. Основная идея

Шардирование = **routing по ключу**.  
Ты выбираешь ключ (например, `user_id`),  
и каждая строка попадает в _определённый шард_.

Пример:

```
user_id % 4 → номер шарда (0..3)
```

- `user_id = 1, 5, 9` → shard 1
    
- `user_id = 2, 6, 10` → shard 2
    
- и т.д.
    

> Главное — правильно выбрать ключ, чтобы нагрузка распределялась равномерно.

---

### 3. Как выглядит шардированная схема

```
              ┌────────────┐
              │ Router/API │  ← решает, куда писать/читать
              └──────┬─────┘
                     │
 ┌────────────────────────────────────┐
 │            PostgreSQL             │
 ├─────────────┬────────────┬─────────┤
 │ Shard 1     │ Shard 2    │ Shard 3 │
 │ users_1..10 │ users_11.. │ users_..│
 └─────────────┴────────────┴─────────┘
```

Каждый шард — **отдельный инстанс PostgreSQL** со своей схемой и WAL-журналом.  
Иногда используют “центральный каталог” (metadata DB) для хранения карты шардов.

---

### 4. Пример ручного шардирования

#### 1. Несколько баз

```
db_user_shard_0
db_user_shard_1
db_user_shard_2
db_user_shard_3
```

#### 2. Функция выбора шарда:

```php
function getShardId(int $userId): int {
    return $userId % 4;
}
```

#### 3. ORM или DAL обращается к нужной базе:

```php
$shard = getShardId($userId);
$connection = $connections["db_user_shard_{$shard}"];
$user = $connection->query("SELECT * FROM users WHERE id = ?", [$userId]);
```

> Приложение отвечает за маршрутизацию,  
> PostgreSQL сам по себе “не умеет” шардировать данные.

---

### 5. Варианты шардирования

|Тип|Принцип|Пример|
|---|---|---|
|**Hash sharding**|равномерное распределение по хэшу ключа|`user_id % 4`|
|**Range sharding**|диапазоны значений|`user_id BETWEEN 1–1M`, `1M–2M`|
|**Geo sharding**|по региону|`region='EU' → shard_eu`|
|**Functional / hybrid**|логическая логика|`customers → shard1`, `orders → shard2`|

---

### 6. Поддержка в PostgreSQL

PostgreSQL **из коробки не имеет встроенного шардирования**,  
но можно использовать несколько подходов:

#### ✅ **Foreign Data Wrappers (FDW)**

Позволяют подключать внешние таблицы из других БД:

```sql
CREATE EXTENSION postgres_fdw;
CREATE SERVER shard1 FOREIGN DATA WRAPPER postgres_fdw OPTIONS (host '10.0.0.1', dbname 'users_1');
CREATE FOREIGN TABLE users_1 (...) SERVER shard1 OPTIONS (table_name 'users');
```

→ Можно объединять запросы `UNION ALL` через внешние источники.  
Минус — не масштабируется идеально и сложен в поддержке.

#### ✅ **Citus**

Расширение для PostgreSQL, превращающее его в распределённую СУБД.  
Автоматически делает распределённые таблицы и маршрутизацию.

```sql
CREATE EXTENSION citus;
SELECT create_distributed_table('orders', 'user_id');
```

Citus сам определяет шард, выполняет запрос и агрегирует результат.  
(Сейчас принадлежит Microsoft и активно развивается.)

---

### 7. Проблемы шардирования

|Проблема|Почему сложно|
|---|---|
|JOIN между шардами|нужно собирать данные вручную|
|Пересечение транзакций|ACID-гарантии ограничены в пределах одного шарда|
|Перешардирование|при добавлении ноды нужно перемещать данные|
|Балансировка|возможна неравномерная нагрузка (hot shard)|
|Агрегации и аналитика|нужно собирать результаты со всех шардов|

> Поэтому важно изначально выбрать правильный шард-ключ —  
> чаще всего это `tenant_id`, `user_id` или `account_id`.

---

### 8. Отличие от партиционирования

|Характеристика|Партиционирование|Шардирование|
|---|---|---|
|Уровень|Внутри одной БД|Между несколькими БД|
|Управление|PostgreSQL сам|Приложение или middleware|
|Сложность|Простое|Сложное|
|JOIN’ы|Работают|Требуют агрегации|
|Масштабирование|Ограничено сервером|Горизонтальное (без лимита)|

---

### 9. Инструменты для шардирования

|Инструмент|Особенности|
|---|---|
|**Citus**|расширение PostgreSQL, автоматическое распределение|
|**Pgpool-II**|middleware с read/write split и простым routing|
|**PostgreSQL FDW**|“встроенный” способ, ручная маршрутизация|
|**Vitess**|изначально для MySQL, но аналогичные идеи (shard router)|
|**Custom logic в приложении**|частый вариант для SaaS (ручной routing)|

---

### 10. Когда применять

✅ Когда:

- у тебя **десятки миллионов пользователей** или **терабайты данных**;
    
- упёрся в RAM / IOPS / CPU одного сервера;
    
- нужно обслуживать **глобальные регионы**;
    
- у тебя **SaaS-модель**, где данные разных клиентов можно изолировать по шардам.
    

❌ Не стоит:

- если нагрузка помещается в один сервер;
    
- если приложение активно делает JOIN’ы и агрегации;
    
- если нет стабильного ключа для деления.
    

---

### 💡 В одной фразе

> **Шардирование** — это когда база становится “кластером баз”.  
> Вместо того чтобы делать одну огромную таблицу,  
> ты делаешь много маленьких и заставляешь приложение знать, где искать данные.

[[Partitioning (Партиционирование) в PostgreSQL]]
[[Репликация (replication) баз данных (на примере PostgreSQL)]]