
**Хранимка** — это кусок кода (обычно SQL или PL/pgSQL),  
который выполняется **на стороне базы данных**, а не приложения.  
База хранит её “в себе” и может вызывать по имени, как функцию.

---

### 1. Зачем они нужны

- Выполнять сложную бизнес-логику **внутри базы**, без клиента.
    
- Минимизировать сетевые запросы (всё выполняется внутри одной транзакции).
    
- Реализовать периодические задачи, валидации, каскады, агрегации.
    
- Дать API к данным без прямого доступа к таблицам (например, для отчётности или BI).
    

> По сути, это “бэкенд” на SQL — код, который живёт ближе к данным, чем приложение.

---

### 2. Простой пример (PostgreSQL)

```sql
CREATE OR REPLACE FUNCTION user_stats(user_id int)
RETURNS TABLE(orders int, total numeric) AS $$
BEGIN
  RETURN QUERY
  SELECT count(*), sum(amount)
  FROM orders
  WHERE orders.user_id = user_stats.user_id;
END;
$$ LANGUAGE plpgsql;
```

Вызов:

```sql
SELECT * FROM user_stats(42);
```

---

### 3. Разница между FUNCTION и PROCEDURE

|Тип|Возвращает|Можно использовать в SELECT|Транзакции|
|---|---|---|---|
|**FUNCTION**|Результат (scalar/table)|✅ Да|управляется вызывающим кодом|
|**PROCEDURE**|Ничего / OUT-параметры|❌ Нет|может сама делать `COMMIT/ROLLBACK`|

```sql
CREATE PROCEDURE cleanup_old_orders(days int)
LANGUAGE plpgsql
AS $$
BEGIN
  DELETE FROM orders WHERE created_at < now() - (days || ' days')::interval;
  COMMIT;
END;
$$;
```

---

### 4. Как это работает под капотом

- Хранимка **компилируется при создании** и сохраняется в системных таблицах (`pg_proc`).
    
- При первом вызове база **создаёт план выполнения** и кэширует его.
    
- При повторных вызовах запрос выполняется быстрее (reusing execution plan).
    
- Хранимка исполняется **внутри сессии**, может обращаться к любой таблице,  
    изменять данные, вызывать другие функции и триггеры.
    

Если используется `LANGUAGE plpgsql`, Postgres запускает встроенный интерпретатор,  
но также можно писать на `LANGUAGE sql`, `plpythonu`, `plperl` и др.

---

### 5. Примеры полезных применений

#### ✅ Агрегации и отчёты

```sql
CREATE FUNCTION daily_signups(date)
RETURNS int AS $$
SELECT count(*) FROM users WHERE created_at::date = $1;
$$ LANGUAGE sql;
```

#### ✅ Автоматизация (очистка, архивирование)

```sql
CREATE PROCEDURE archive_logs()
LANGUAGE plpgsql
AS $$
BEGIN
  INSERT INTO logs_archive SELECT * FROM logs WHERE created_at < now() - interval '30 days';
  DELETE FROM logs WHERE created_at < now() - interval '30 days';
END;
$$;
```

#### ✅ Сервисный API

Можно создавать “контракты” между приложением и базой —  
например, вместо `INSERT` напрямую:

```sql
CALL create_order(123, 599.00);
```

База сама проверит и вставит данные в нужные таблицы.

---

### 6. Потенциальные минусы

⚠️ **Трудно тестировать и версионировать**  
(особенно без системы миграций, вроде Flyway или Liquibase).

⚠️ **Сложнее отлаживать** — логика скрыта в базе.

⚠️ **Привязывают к конкретной СУБД** (PostgreSQL ≠ MySQL ≠ MSSQL).

⚠️ **Часто злоупотребляют** — бизнес-правила начинают жить в двух местах (в коде и в БД).

---

### 7. Просмотр и управление

```sql
-- список функций
\dF
-- описание конкретной
\df+ user_stats
-- удалить
DROP FUNCTION user_stats(int);
```

---

### 8. Когда оправдано

✅ Хранимки полезны:

- для отчётности, агрегаций, аналитики;
    
- для задач, близких к данным (архивация, валидация);
    
- для кросс-языковых ограничений (одна база, много клиентов).
    

🚫 Не стоит использовать:

- для бизнес-логики, которая уже реализована в приложении;
    
- для частых правок и быстро меняющегося поведения.
    

