**i18n** — (_internationalization_) — практика **отделения текстов, форматов дат, валют и чисел от логики кода**,  
чтобы приложение можно было легко адаптировать под разные языки и регионы.

---

### 1. Зачем это нужно

- Поддержка нескольких языков (ru / en / de / fr).
    
- Разные форматы дат, чисел и валют.
    
- Правильные формы слов (1 комментарий, 2 комментария, 5 комментариев).
    
- Возможность менять тексты без правок в коде.
    

> Даже если язык один, выносить тексты в файлы всё равно полезно —  
> бизнес часто просит “точку заменить на запятую” или “переписать уведомление”,  
> и править YAML-файл куда безопаснее, чем лазить по коду.

---

### 2. Основные термины

|Термин|Значение|
|---|---|
|**i18n**|Интернационализация — подготовка приложения к многоязычности.|
|**l10n**|Локализация — конкретные переводы и форматы.|
|**Locale**|Язык и регион, например `ru_RU`, `en_US`.|
|**Pluralization**|Правила множественных форм слов.|
|**Translation key**|Ключ перевода (`user.login.success`).|

---

### 3. Пример структуры переводов (YAML)

#### ❌ Вариант с вложенными ключами

```yaml
items_count:
  one: "You have 1 item"
  other: "You have %count% items"
```

Так писать можно, но плохо ищется:  
`grep -R "items_count.one"` ничего не найдёт,  
а IDE не подсвечивает совпадения по полному ключу.

#### ✅ Лучше использовать плоские ключи

```yaml
items_count.one: "You have 1 item"
items_count.other: "You have %count% items"
```

Теперь `grep` или поиск по IDE (`items_count.one`) найдёт все вхождения в коде и YAML-файлах.  
Это упрощает навигацию, поиск по проекту и массовое обновление ключей.

---

### 4. Пример перевода для нескольких языков

```yaml
# translations/messages.en.yaml
welcome: "Welcome, %name%!"
items_count.one: "You have 1 item"
items_count.other: "You have %count% items"

# translations/messages.ru.yaml
welcome: "Привет, %name%!"
items_count.one: "У тебя %count% предмет"
items_count.few: "У тебя %count% предмета"
items_count.many: "У тебя %count% предметов"
```

В коде:

```php
$this->translator->trans('items_count.many', ['%count%' => 5]);
```

---

### 5. Расширение `intl` в PHP

Модуль **`intl`** обеспечивает функции локализации и форматирования  
через библиотеку **ICU (International Components for Unicode)**.

Нужен для:

- **Плюрализации** (`MessageFormatter`, `NumberFormatter`),
    
- **Форматирования дат и валют**,
    
- **Сравнения строк с учётом языка**.
    

Пример:

```php
$fmt = new \MessageFormatter('ru_RU', '{count, plural, one{# комментарий} few{# комментария} many{# комментариев} other{# комментария}}');
echo $fmt->format(['count' => 5]); // 5 комментариев
```

📘 Документация:

- [PHP intl (MessageFormatter)](https://www.php.net/manual/en/class.messageformatter.php)
    
- [ICU Plural Rules](https://unicode-org.github.io/cldr-staging/charts/latest/supplemental/language_plural_rules.html)
    

---

### 6. Практические рекомендации

- Все тексты и форматы — **в отдельных YAML-файлах**, не в коде.
    
- Используй **плейсхолдеры** (`%name%`, `%count%`) вместо конкатенации строк.
    
- Настрой **fallback locale** (например, `en`).
    
- Для чисел и дат — `IntlDateFormatter`, `NumberFormatter`.
    
- Проверяй pluralization под свой язык: в русском — **три формы** (`1`, `2–4`, `5+`).
    
- Предпочитай **плоские ключи**: это облегчает поиск и рефакторинг.
    

---

### 💡 В одной фразе

> **i18n** — это не про переводы, а про дисциплину:  
> отделяешь текст от кода — экономишь время, когда бизнес просит «поменять одно слово».