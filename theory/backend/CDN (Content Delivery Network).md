**CDN** — это распределённая сеть серверов,  
которая доставляет статический контент (изображения, JS, CSS, видео, файлы)  
пользователю **с ближайшего узла**, а не с оригинального сервера.

> Главная цель CDN — **уменьшить задержку (latency)** и **разгрузить основной сервер**.

---

### 1. Как это работает

1. Пользователь запрашивает ресурс (например, `https://cdn.example.com/logo.png`).
    
2. Запрос идёт не к твоему серверу, а к ближайшей CDN-ноде.
    
3. Если файл есть в кеше — возвращается мгновенно.
    
4. Если нет — CDN подтягивает его с origin-сервера, кэширует и отдаёт пользователю.
    

> При следующих запросах этот же контент отдается уже локально — без похода к origin.

---

### 2. Что кэшируется

- Изображения, шрифты, CSS, JS, видео.
    
- API-ответы (если они публичные и редко меняются).
    
- Страницы, собранные статически (например, HTML у SPA).
    

> Важно: приватные данные **не кэшируются** на CDN.

---

### 3. Основные преимущества

|Преимущество|Что даёт|
|---|---|
|⚡ Быстрее загрузка|данные ближе к пользователю|
|🧠 Меньше нагрузки|origin-сервер обрабатывает только “уникальные” запросы|
|🛡️ Защита|скрывает реальный IP origin-сервера|
|💸 Экономия|CDN часто дешевле по трафику, чем origin|
|📈 Масштабирование|выдерживает всплески нагрузки без падений|

---

### 4. Как CDN решает “больные места”

#### Без CDN:

- Пользователь из Вьетнама тянет файлы с Франции.
    
- 200 мс RTT, тормоза, таймауты, слёзы.
    

#### С CDN:

- CDN имеет POP (Point of Presence) в Ханое.
    
- Первый запрос: кэшируется локально.
    
- Все следующие — 10 мс и всё летает.
    

---

### 5. Ключевые термины

|Термин|Значение|
|---|---|
|**POP (Point of Presence)**|Узел CDN в конкретном регионе|
|**Edge server**|Сервер, обслуживающий конечного пользователя|
|**Origin**|Твой исходный сервер|
|**TTL (Time To Live)**|Время, на которое объект хранится в кеше|
|**Cache hit / miss**|Попал ли запрос в кэш или нет|
|**Purge / Invalidate**|Принудительное удаление из кэша при обновлении контента|

---

### 6. Когда использовать CDN

✅ Если у тебя есть:

- Пользователи по всему миру
    
- Много статического контента
    
- Высокие пиковые нагрузки
    
- API-ответы, которые редко меняются
    

❌ Не нужно CDN, если:

- Приложение полностью приватное и работает только внутри одного офиса
    
- Нет статики, только динамические персонализированные ответы
    

---

### 7. Примеры популярных CDN

|Платформа|Особенности|
|---|---|
|**Cloudflare**|самый популярный, бесплатный тариф, DDoS-защита|
|**Fastly**|низкие задержки, гибкие правила кэширования|
|**AWS CloudFront**|интеграция с S3, Route53, Lambda@Edge|
|**Akamai**|промышленный стандарт, гигантская сеть|
|**Bunny.net**|дешёвый, простой интерфейс, часто для проектов среднего размера|

---

### 8. CDN и HTTP-заголовки

HTTP-заголовки определяют, как CDN кэширует контент:

```http
Cache-Control: public, max-age=3600
ETag: "v2.5.1"
Vary: Accept-Encoding
```

- `Cache-Control` — срок жизни кэша
    
- `ETag` — уникальная метка версии
    
- `Vary` — позволяет хранить разные версии под разные параметры
    

---

### 9. Связка с другими технологиями

|Технология|Роль|
|---|---|
|**[[S3]] / MinIO**|хранение файлов (origin для CDN)|
|**[[Nginx]]**|может работать как собственный кэширующий reverse-proxy|
|**[[Prometheus]] / [[Grafana]]**|мониторинг cache hit ratio, latency|
|**[[CI CD|CI/CD]]**|автоматический purge CDN после деплоя|

---

### 🔗 Связанные заметки

- [[HTTP & HTTPS]] — базовый протокол, на уровне заголовков которого управляется кэширование (`Cache-Control`, `ETag`).
- [[Стратегии кеширования (Cache Strategies)]] и [[Стратегии вытеснения кеша (Cache Eviction Policies)]] — помогают выбирать правила хранения и протоколы обновления контента на edge-узлах.
- [[Прогрев кеша]] — о том, как заранее заполнить CDN и application cache, чтобы избежать холодного старта.

---

### 10. Потенциальные проблемы

- Старый контент может “залипнуть” в кэше → нужно делать `purge`.
    
- Сложно кэшировать динамические ответы.
    
- Неправильные заголовки могут сделать контент “вечным”.
    
- Не все CDN одинаково хорошо обрабатывают HTTPS и CORS.
    

---

### 💡 В одной фразе

> **CDN** — это глобальный кэш твоего контента на “передовой”.  
> Он делает сайты быстрыми, сервера счастливыми, а пользователей — терпеливыми.