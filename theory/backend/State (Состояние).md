**State (состояние)** — это всё, что _меняется со временем_.  
То, что хранит контекст: данные пользователя, текущий шаг, кеш, сессию, счётчик, и т.п.

Состояние — главная причина непредсказуемого поведения и багов.  
Когда данные можно менять из разных мест, приложение перестаёт быть детерминированным:  
ты уже не уверен, _кто именно_ изменил значение и _когда_.

---

### 1. Почему “state” — зло

- **Невидимые зависимости** — функция использует переменные, которые не переданы явно.
    
- **Побочные эффекты** — одна часть системы меняет данные, и другая внезапно ломается.
    
- **Сложно тестировать** — результат зависит не только от входных параметров, но и от контекста.
    
- **Сложно масштабировать** — нужно синхронизировать состояние между процессами.
    

> Чем меньше состояния — тем проще код и тем выше надёжность.

---

### 2. Как разные экосистемы решают проблему

#### **React / Frontend**

React построен вокруг идеи **единого источника правды**.  
Всё состояние должно храниться в одной точке — в `state` или `store` (например, Redux).  
Компоненты не изменяют данные напрямую, а получают snapshot и события обновления.

Так достигается **предсказуемость** — любое состояние можно “воспроизвести” из входных данных и истории действий.

#### **Clojure / Функциональные языки**

В Clojure и других функциональных языках состояние почти полностью **иммутабельно**.  
Обычно изменяемым может быть **только один объект** (`atom`, `ref`, `agent`),  
всё остальное — копии, а не мутации.

> Вместо “изменить объект” — создаётся новая версия данных.  
> Это упрощает параллелизм, потому что ничего нельзя “случайно” испортить.

#### **PHP / Backend**

В PHP большинство проблем со state связаны с тем, что объекты по умолчанию **мутабельны**.  
Использование `readonly` свойств и DTO помогает писать более безопасный, чистый код.

```php
readonly class UserDto {
    public function __construct(
        public string $email,
        public string $name
    ) {}
}
```

Это особенно важно, если ты планируешь использовать **RoadRunner (RR)** —  
где приложение живёт долго, и старое состояние может “просочиться” в новый запрос.  
В таких системах “глобальные переменные” буквально приводят к утечкам и ошибкам.

---

### 3. Stateless — идеал REST

REST-принцип “**stateless**” означает:

> каждый запрос содержит всё, что нужно для его обработки,  
> и не зависит от состояния на сервере.

Почему это важно:

- можно масштабировать горизонтально — любой сервер может обработать запрос;
    
- нет необходимости хранить сессии в памяти;
    
- предсказуемость: один запрос → один ответ, без контекста.
    

Пример:  
вместо `GET /user/current`, где сервер хранит “кто сейчас вошёл”,  
используется `GET /user/{id}`, где `id` явно передаётся в запросе.

---

### 4. Хорошие практики

✅ Используй **иммутабельные объекты** (или `readonly`).
✅ Данные должны быть **внешними** по отношению к коду — храниться в БД, Redis, файле.
✅ Не полагайся на **глобальные переменные** и синглтоны.
✅ Для API — проектируй **stateless** контракты.
✅ Для фронтенда — храни состояние централизованно, а не в каждом компоненте.

---

### 🔗 Связанные заметки

- [[Сессии (Sessions)]] — когда state неизбежен, важно понимать, как хранить и масштабировать пользовательские сессии.
- [[rest|REST и RESTful API]] — поясняет, что означает принцип stateless и как его применять в HTTP-контрактах.
- [[RoadRunner]] — рантайм, где долгоживущие процессы усиливают проблемы с “протекающим” состоянием.
- [[Event Sourcing]] и [[Domain-Driven Design (DDD)]] — подходы к проектированию домена, в которых состояние контролируется через события и инварианты.

---

### 💡 В одной фразе

> **State** — это источник хаоса.
> Хороший код не “живёт”, он просто **вычисляется**:
> из входных данных → в выходные, без памяти о прошлом.
