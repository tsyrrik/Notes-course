**JWT** — это способ передавать информацию о пользователе (например, что он залогинен)  
в виде **подписанного JSON-объекта**.  
Он используется для **stateless-аутентификации** — когда сервер не хранит сессии,  
а всё, что нужно, передаётся в токене.

---

### 1. Пример токена

```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.
eyJzdWIiOjEyMywicm9sZSI6ImFkbWluIiwiZXhwIjoxNzQxMzA4MDAwfQ.
hT2kOiLRO5DjeWb3ItJG0FrpK9wYHPrgP13oLJXPUnE
```

#### Header

```json
{ "alg": "HS256", "typ": "JWT" }
```

#### Payload

```json
{
  "sub": 123,
  "role": "admin",
  "exp": 1741308000
}
```

#### Signature

```
HMACSHA256(
  base64UrlEncode(header) + "." + base64UrlEncode(payload),
  secret
)
```

Проверить и декодировать токен можно на [jwt.io](https://jwt.io/).

---

### 2. Как это работает

1. Клиент логинится → сервер генерирует JWT.
    
2. Клиент сохраняет токен (например, в `Authorization: Bearer <token>`).
    
3. Каждый запрос отправляется с токеном.
    
4. Сервер проверяет подпись, срок действия (`exp`) и, если всё корректно — считает пользователя авторизованным.
    

> Сервер **не хранит состояние**, поэтому JWT идеален для API и микросервисов.

---

### 3. Преимущества перед сессиями

|Особенность|Сессии|JWT|
|---|---|---|
|Хранение|В Redis / БД|На клиенте|
|Масштабирование|Нужен общий session storage|Любой сервер может проверить токен|
|Проверка|Нужен доступ к БД|Проверяется локально по подписи|
|Производительность|+1 запрос к хранилищу|1 CPU-операция|
|Безопасность выхода|Очистка сессии|Можно отозвать конкретный токен по `jti`|
|Распределённые API|Сложно|Идеально|

Пример: если у тебя 5 API-серверов, JWT позволит им работать независимо.  
Каждый умеет проверять токен локально по публичному ключу, не обращаясь к Redis.  
Это **меньше нагрузки на базу** и проще масштабирование.

---

### 4. Почему токен нельзя подделать

#### Подпись

JWT подписывается **секретом** (при `HS256`) или **приватным ключом** (при `RS256`).

```
signature = Sign( base64Url(header) + "." + base64Url(payload), key )
```

Сервер проверяет подпись, пересчитывая её своим ключом.  
Если хоть один байт payload изменён — подпись уже не совпадает.

#### Что защищает токен:

- **Секрет / приватный ключ**: злоумышленник не знает, чем подписано.
    
- **Алгоритм**: сервер сам задаёт допустимый (`HS256`, `RS256`), а не доверяет `alg` из токена.
    
- **`exp`, `nbf`, `iat`**: ограничивают время жизни.
    
- **HTTPS**: предотвращает перехват в пути.
    
- **HttpOnly cookies**: защищают от XSS.
    

Без знания секрета / приватного ключа токен нельзя пересобрать с валидной подписью.

📘 Подробное описание стандарта: [RFC 7519 — JSON Web Token (JWT)](https://datatracker.ietf.org/doc/html/rfc7519)

---

### 5. Claims (поля токена)

|Поле|Значение|
|---|---|
|`iss`|кто выдал токен (issuer)|
|`sub`|идентификатор пользователя|
|`aud`|кому предназначен токен|
|`exp`|время истечения|
|`nbf`|"не раньше чем"|
|`iat`|время выпуска|
|`jti`|уникальный идентификатор токена|

Можно добавлять свои: `role`, `tenant_id`, `email`, `permissions` и т. д.

---

### 6. Refresh и отзыв токенов

JWT обычно живёт 15–60 минут.  
Для продления используется **refresh token** — он живёт дольше и может выдать новую пару токенов.

Чтобы “выйти со всех устройств” или **заблокировать конкретный токен**, можно хранить `jti` (идентификатор токена) в базе:

- при logout — пометить `jti` как отозванный,
    
- при проверке токена — проверять blacklist.
    

Так пользователь может “выкинуть” другие устройства,  
что в сессионной модели часто сложнее реализовать.

---

### 7. Пример в PHP (через Firebase\JWT)

```php
use Firebase\JWT\JWT;
use Firebase\JWT\Key;

$secret = $_ENV['JWT_SECRET'];

$payload = [
  'sub' => $user->id,
  'role' => $user->role,
  'iat' => time(),
  'exp' => time() + 3600,
  'jti' => bin2hex(random_bytes(8))
];

$token = JWT::encode($payload, $secret, 'HS256');

// Проверка токена
try {
  $decoded = JWT::decode($token, new Key($secret, 'HS256'));
  $userId = $decoded->sub;
} catch (Exception $e) {
  throw new UnauthorizedException('Invalid token');
}
```

---

### 8. Типичные ошибки

- Не проверять `alg` → атака с `alg: none`.
    
- Использовать короткий или предсказуемый `secret`.
    
- Не проверять `exp`, `iss`, `aud`.
    
- Хранить токен в `localStorage` (уязвим к XSS).
    
- Не использовать HTTPS.
    
- Слишком большой payload (чем больше, тем медленнее каждый запрос).
    

---

### 9. Где JWT особенно полезен

✅ REST API и GraphQL.
✅ Микросервисы (проверка подписи локально).
✅ SSO — один токен для нескольких систем.
✅ WebSocket / mobile — без cookie и сессий.

---

### Связанные заметки

- [[OAuth 2.0]] — про то, как выдаются access/refresh токены и какие у них потоки.
- [[Сессии (Sessions)]] — альтернатива с хранением состояния на сервере и cookie.
- [[State (Состояние)]] — почему stateless-подход упрощает масштабирование.

