**Сессия** — это способ сохранить состояние между HTTP-запросами.  
Поскольку HTTP сам по себе **stateless**, сервер не “помнит”, кто к нему обращался.  
Сессии решают эту проблему, связывая несколько запросов с одним пользователем.

---

### 1. Как это работает

1. Пользователь логинится (например, POST `/login`).
    
2. Сервер создаёт **сессию** и сохраняет в хранилище (БД, Redis, файлы):
    
    ```json
    {
      "session_id": "abc123",
      "user_id": 42,
      "expires_at": "2025-11-08T12:00:00"
    }
    ```
    
3. Клиент получает cookie:
    
    ```
    Set-Cookie: PHPSESSID=abc123; HttpOnly; Secure; SameSite=Lax
    ```
    
4. На каждом последующем запросе браузер отправляет этот cookie:
    
    ```
    Cookie: PHPSESSID=abc123
    ```
    
5. Сервер находит запись по `session_id`, понимает, кто пользователь, и продолжает работу.
    

---

### 2. Где хранятся сессии

|Хранилище|Особенности|
|---|---|
|**Файлы (по умолчанию)**|просто, но плохо масштабируется|
|**Redis / Memcached**|быстро и подходит для кластера|
|**PostgreSQL / MySQL**|надёжно, но медленнее|
|**In-memory**|хорошо для временных авторизаций|

В PHP, Symfony и Laravel это всё настраивается через session handler.

---

### 3. Что лежит внутри сессии

Сессия — это просто **key-value хранилище**,  
где ключ = `session_id`, а значение = сериализованные данные.

Например:

```php
[
  '_security_main' => 'User{id:42, role:admin}',
  'cart' => [12, 15, 33]
]
```

> Сервер при каждом запросе достаёт эти данные и подставляет их в контекст пользователя.

---

### 4. Под капотом в PHP

PHP сам:

- генерирует `session_id` (обычно 32 символа);
    
- сохраняет данные (в `/tmp` или через handler);
    
- добавляет заголовок `Set-Cookie`;
    
- автоматически восстанавливает сессию при следующем запросе.
    

```php
session_start();
$_SESSION['user_id'] = 42;
echo $_SESSION['user_id']; // 42
```

> Сессия живёт, пока не истечёт `session.gc_maxlifetime` или пока её не удалят вручную.

---

### 5. Преимущества

✅ Проста в реализации — “из коробки” работает во всех фреймворках.  
✅ Cookie автоматически передаются браузером.  
✅ Можно хранить произвольные данные.  
✅ Легко “разлогинить” пользователя — просто удалить запись из хранилища.

---

### 6. Недостатки

⚠️ **Нагрузочная точка** — каждое обращение требует чтения из БД/Redis.  
⚠️ **Не масштабируется без общего хранилища** — серверы должны делить одну базу сессий.  
⚠️ **Привязка к серверу** — не подходит для stateless API и микросервисов.  
⚠️ **Безопасность cookie** — при XSS или утечке cookie злоумышленник может украсть сессию.  
⚠️ **Тяжело использовать в мобильных и внешних клиентах (cookie не всегда доступны).**

---

### 7. Сравнение с JWT

|Критерий|Сессия|JWT|
|---|---|---|
|Хранение|На сервере|На клиенте|
|Проверка|Через хранилище|Через подпись|
|Масштабирование|Требует Redis/БД|Stateless|
|Отзыв токена|Просто|Нужно реализовать вручную|
|Размер|Маленький cookie|До 2 KB|
|API без cookie|Неудобно|Идеально|

> Сессия = хранение состояния на сервере.  
> JWT = перенос состояния в токен и подпись.

---

### 8. Когда сессии уместны

✅ Классические веб-приложения с cookie (панель администратора, e-commerce).  
✅ Небольшие сайты, где есть один сервер и не нужна масштабируемость.  
✅ Сценарии, где нужна мгновенная инвалидизация (logout = delete).

🚫 Не подходят для:

- REST API без cookie,
    
- мобильных и SPA-клиентов,
    
- микросервисов.
    

---

### 9. Безопасность

- Используй `HttpOnly`, `Secure`, `SameSite` в cookie.
    
- Не храни чувствительные данные прямо в сессии.
    
- При смене роли / прав — сбрасывай сессию (`session_regenerate_id`).
    
- Не делись сессиями между пользователями или поддоменами.


---
### Связанные заметки

- [[JWT (JSON Web Token)]] — stateless-альтернатива, где данные о пользователе хранятся в токене.
- [[OAuth 2.0]] — про делегированную авторизацию и работу с access token.
- [[State (Состояние)]] — зачем минимизировать состояние и как это влияет на архитектуру.

---
### 💡 В одной фразе

> **Сессия** — это запись на сервере, которая хранит, кто ты и что делал.  
> Проста и надёжна для классических сайтов,  
> но плохо подходит для распределённых и stateless-систем.