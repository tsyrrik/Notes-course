**Cron** — это планировщик задач в Unix/Linux, который выполняет команды **по расписанию**.  
Обычно используется для бэкапов, очисток, отчётов, запусков скриптов и т.п.

---

### 1. Формат crontab

```
# ┌───────── минута (0–59)
# │ ┌─────── час (0–23)
# │ │ ┌───── день месяца (1–31)
# │ │ │ ┌─── месяц (1–12)
# │ │ │ │ ┌─ день недели (0–7, 0 и 7 = воскресенье)
# │ │ │ │ │
# * * * * * команда
```

Примеры:

```bash
* * * * * php /var/www/app/bin/console schedule:run >> /var/log/cron.log 2>&1
0 3 * * * bash /opt/backup.sh
```

🧠 Подробный генератор расписаний:  
👉 [crontab.guru](https://crontab.guru/)

---

### 2. Ограничения

- **Минимальная точность — 1 минута.**  
    Cron не умеет запускать задачи чаще, чем раз в минуту.
    
- **Он не убивает процесс, если тот не успел.**  
    Cron просто не запустит _следующий запуск_, пока предыдущий ещё выполняется,  
    если используется стандартный `cronie` или `vixie-cron`.  
    (то есть _параллельно_ не стартует, но _насильно не убивает_).
    
    ✅ Хорошая практика — писать скрипт с file-lock (например, `flock`)  
    чтобы не запускать несколько экземпляров.
    
    ```bash
    * * * * * flock -n /tmp/schedule.lock php bin/console schedule:run
    ```
    

---

### 3. Управление

```bash
crontab -e   # редактировать задания
crontab -l   # показать список
crontab -r   # удалить все задания
```

---

### 4. Альтернативы cron

|Подход|Пример|Когда лучше|
|---|---|---|
|**Symfony Scheduler / Laravel Schedule**|`bin/console schedule:run` / `php artisan schedule:run`|планирование внутри приложения|
|**Очереди задач (Queue workers)**|Symfony Messenger, Laravel Queues|фоновые и повторяющиеся задачи с контролем частоты|
|**Systemd timer**|`.timer` юнит в Linux|надёжная замена cron с логами и контрольным временем|
|**Supervisord**|для постоянных процессов|для непрерывно работающих скриптов|

---

### 5. Логи

По умолчанию cron отправляет вывод на e-mail пользователя.  
Чтобы писать в файл:

```bash
* * * * * php script.php >> /var/log/cron.log 2>&1
```

