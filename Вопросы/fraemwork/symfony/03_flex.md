# Symfony Flex

Простыми словами: Flex управляет зависимостями и "рецептами" — автогенерирует конфиги, папки, пакеты при установке через Composer.

```bash
composer create-project symfony/skeleton myapp
composer require orm
composer require symfony/twig-pack
```
